# Fix Wave Plan — CopyPaste v0.1.0-alpha.1
**Planner:** system-architect
**Date:** 2026-05-23
**Source audits:** security (21), best-practices (28), architecture (27), edge-cases (38) — 114 findings total
**Findings selected to fix:** 35 (Critical: 8, High: 17, Medium: 10)
**Total waves:** 3
**Workers per wave:** Wave 1 = 6, Wave 2 = 9, Wave 3 = 7
**Estimated wall-time (parallel):** ~3 hours total (1h per wave + verify)

---

## Severity inventory

| Source       | Critical (in) | High (in) | Medium (in) | Deferred |
|--------------|---------------|-----------|-------------|----------|
| security     | 2 → 2         | 6 → 6     | 7 → 4       | 0        |
| edge-cases   | 4 → 4         | 11 → 7    | 13 → 0      | low/info |
| best-prac    | 0             | 7 → 4     | 11 → 2      | low/info |
| architecture | 4 → 2*        | 7 → 0**   | 9 → 4       | rest     |

*Architecture CRITICAL #3 (orphan crates), #2 (UI type drift), #4 (relay phantom DB) → deferred to post-alpha (documented in README).
**Architecture HIGH findings are mostly invasive refactors → deferred.

---

## Wave 1 — CRITICAL blockers (run all 6 in parallel)

### Worker 1.1 — IPC daemon socket + request hardening
**Files (exclusive):**
- `crates/copypaste-daemon/src/ipc.rs`
- `crates/copypaste-daemon/src/daemon.rs`

**Findings to fix:**
- security CRITICAL #5: Unix socket world-connectable → `set_permissions(0o600)` after `UnixListener::bind`; also `0o700` on parent dir
- security HIGH #6 / edge-cases CRITICAL #1: `BufReader::lines()` unbounded → switch to `read_until(b'\n', &mut buf)` with `MAX_REQUEST_BYTES = 16 MiB`, reject and close on overflow
- security HIGH #4: `peers.json` plaintext, no chmod → `set_permissions(0o600)` after every `std::fs::write`; same for `config.json`
- best-prac MEDIUM #11: `serde_json::from_str(&s).ok().unwrap_or_default()` swallows config errors → log `tracing::warn!`
- best-prac MEDIUM #12: `dirs::config_dir().unwrap_or_else(|| ".")` silent CWD fallback → log warn-once
- edge-cases HIGH #10: client disconnect mid-request (`read_line` → `Ok(0)`) → ensure handler does not panic on closed socket

**Tests to add (3):**
- `ipc_socket_chmod_is_0600()`
- `ipc_oversized_request_rejected_not_crashed()`
- `ipc_client_mid_request_disconnect_does_not_panic()`

---

### Worker 1.2 — Relay auth (deterministic token) hardening
**Files (exclusive):**
- `crates/copypaste-relay/src/state.rs`
- `crates/copypaste-relay/src/routes/devices.rs`
- `crates/copypaste-relay/src/routes/mod.rs`

**Findings to fix:**
- security CRITICAL #1: bearer token derived from public_key → replace with 32 random bytes from `OsRng`, base64-encoded; never expose raw token via list endpoint (return only `device_id`+`name`+`public_key_b64`); persist random token only in the device record
- security MEDIUM #14: no proof-of-possession on `POST /devices` → at minimum reject zero-length / malformed public_key_b64; PoP signature is deferred to v0.2 but document explicitly
- security INFO #21 (relay state.rs): replace `.expect("state mutex poisoned")` with `lock().unwrap_or_else(|e| e.into_inner())` on hot paths

**Tests to add (3):**
- `relay_bearer_token_is_random_not_derived_from_pubkey()`
- `relay_get_devices_does_not_leak_bearer_token()`
- `relay_list_endpoint_returns_only_safe_fields()`

---

### Worker 1.3 — Daemon fingerprint + pairing UI safety
**Files (exclusive):**
- `crates/copypaste-daemon/src/keychain.rs`
- `crates/copypaste-daemon/src/p2p.rs`

**Findings to fix:**
- security CRITICAL #2: `get_own_fingerprint` returns fake fingerprint from `DefaultHasher(hostname+pid)` → return the real cert fingerprint from `keychain::load_or_create()` (X25519 keypair) OR mark IPC method as `not_implemented` and have UI display a "Preview — not secure" banner for `PairWindow`
- security MEDIUM #15: p2p IPC methods (`pair_peer`, `list_peers`, `unpair_peer`, `get_own_fingerprint`) are stubs → make them return `Response::not_implemented(req.id, "p2p-pairing")` consistently
- security LOW #18: `tracing::info!("...fingerprint={}")` → demote to `debug!` or first 8 hex chars
- best-prac MEDIUM #18: `keychain.rs:41` ad-hoc fingerprint logging → switch to structured `tracing::info!(fingerprint = %... , "generated new device keypair")`

**Tests to add (2):**
- `get_own_fingerprint_returns_real_cert_fp_or_not_implemented()`
- `pairing_ipc_methods_consistently_return_not_implemented()`

---

### Worker 1.4 — Lamport clock saturation
**Files (exclusive):**
- `crates/copypaste-sync/src/clock.rs`
- `crates/copypaste-sync/src/engine.rs`

**Findings to fix:**
- edge-cases CRITICAL #3: `LamportClock::tick` `+= 1` panics in debug / wraps in release at `u64::MAX` → switch to `saturating_add(1)`; log `warn!` once when saturated
- edge-cases LOW #34: same on wire path (`engine.rs::observe` of incoming `WireItem.lamport = u64::MAX`) → use saturating arithmetic
- edge-cases MEDIUM #16: identical-everything (same lamport+wall+device+payload) merge → idempotent no-op test

**Tests to add (3):**
- `tick_saturates_at_u64_max()`
- `observe_saturates_at_u64_max()`
- `identical_everything_merge_is_idempotent()`

---

### Worker 1.5 — Storage schema downgrade + concurrent writers
**Files (exclusive):**
- `crates/copypaste-core/src/storage/schema.rs`
- `crates/copypaste-core/src/storage/db.rs`
- `crates/copypaste-core/tests/concurrent_writers.rs` (new)

**Findings to fix:**
- edge-cases CRITICAL #2: schema downgrade silent `Ok` → return `SchemaError::Downgrade { found, expected }` when `user_version > SCHEMA_VERSION`
- architecture MEDIUM #15: `apply_migrations` not in a transaction → wrap in `BEGIN; ...; PRAGMA user_version=N; COMMIT;` via `conn.execute_batch`
- edge-cases CRITICAL #4: no concurrent-writers integration test → write `tests/concurrent_writers.rs` spawning 3 tokio tasks (monitor / sync / cloud-push), each insert 1000 items, assert count = 3000 + Lamport monotonicity preserved

**Tests to add (3):**
- `downgrade_returns_explicit_error()`
- `apply_migrations_is_atomic_on_failure()`
- `concurrent_writers_no_lost_updates()` (integration)

---

### Worker 1.6 — Keychain degraded mode + cloud auth fail-closed
**Files (exclusive):**
- `crates/copypaste-daemon/src/cloud.rs`

**Findings to fix:**
- security HIGH #3: Supabase password fail → silent fallback to anon_key → on sign-in failure, abort cloud sync entirely; surface error via IPC channel; never silently downgrade auth scope
- security LOW #20: `SUPABASE_URL` allows `http://` → reject any URL not starting with `https://`; hard-fail with clear message
- security MEDIUM #11 (cloud-side mention): if Keychain unavailable, do NOT silently fall back to ephemeral key when an existing encrypted DB is present. (Daemon-side fix coordinated with W1.5 but cloud.rs handles its own keychain probe.)
- edge-cases HIGH #9: Keychain entry deleted by user/macOS → enter degraded mode + user-visible notification, do not crash-loop

**Tests to add (3):**
- `cloud_signin_failure_aborts_sync_does_not_downgrade()`
- `cloud_rejects_non_https_supabase_url()`
- `keychain_missing_enters_degraded_mode_no_crash_loop()`

---

## Wave 2 — HIGH findings (run all 9 in parallel after Wave 1 verifies)

### Worker 2.1 — Clipboard robustness (rapid changes / mixed types / unsupported)
**Files (exclusive):**
- `crates/copypaste-daemon/src/clipboard.rs`

**Findings to fix:**
- edge-cases HIGH #5: rapid clipboard changes (5 events between polls) → document or expose `ClipboardContent::SkippedBatch` with telemetry log
- edge-cases HIGH #6: mixed text+image — code silently drops image → log `info!("clipboard had text+image; text wins")` and add unit test
- edge-cases HIGH #7: unsupported binary types (RTF, file URLs, custom) → log once per kind, never silent drop
- security LOW #19: image dedup `DefaultHasher` XOR nanos → switch to `Sha256::digest(&raw_bytes)[..16]`

**Tests to add (4):** rapid-change property test, mixed-type wins-text test, unsupported-RTF logs test, image dedup uses sha256 test.

---

### Worker 2.2 — Image deps CVE upgrade
**Files (exclusive):**
- `Cargo.toml` (workspace `[workspace.dependencies]` only)
- `Cargo.lock`
- `crates/copypaste-core/src/image.rs` (only minor signature touch-ups if `image 0.25` API changed)

**Findings to fix:**
- security HIGH #7: upgrade `image 0.24 → 0.25` + `tiff 0.9 → 0.10` (EOL with known OOM CVEs)
- security HIGH #8: upgrade `rusqlite 0.31 → 0.32` (newer SQLCipher); document bundled SQLCipher version in CHANGELOG
- architecture HIGH #11 (partial): collapse `tiff`/`image` duplicates introduced by transitive deps; do NOT touch reqwest/rustls in this wave

**Tests to add:** existing image test suite must pass post-upgrade; add `image_decoder_rejects_oversized_tiff()` regression test.

---

### Worker 2.3 — IPC concurrent clients + early init race + paginated response cap
**Files (exclusive):**
- `crates/copypaste-daemon/tests/integration_ipc.rs`
- `crates/copypaste-daemon/src/ipc.rs` (small touch: pagination cap; coordinate with W1.1 — W1.1 lands first)

**Findings to fix:**
- edge-cases HIGH #11: no test for N=10 concurrent IPC clients
- edge-cases MEDIUM #23: IPC method called before `Database` initialised → return `IPC_NOT_READY` error code, not panic
- edge-cases MEDIUM #24: `list` returning 100k items → enforce server-side pagination cap (`MAX_PAGE = 1000`)

**Tests to add (3):**
- `concurrent_ten_clients_consistent_state()`
- `ipc_call_before_db_ready_returns_error()`
- `list_enforces_max_page_cap()`

**Coordination note:** This worker depends on Wave 1.1 landing first. Schedule second slot of Wave 2.

---

### Worker 2.4 — Crypto chunk gap + AEAD error path
**Files (exclusive):**
- `crates/copypaste-core/src/crypto/encrypt.rs`
- `crates/copypaste-core/src/crypto/chunks.rs`
- `crates/copypaste-core/src/crypto/keys.rs`

**Findings to fix:**
- security MEDIUM #9: HKDF empty salt → use `Hkdf::new(Some(b"copypaste-v1-salt"), &raw_secret)`
- security MEDIUM #10: `expect(...)` on AEAD encrypt → return `EncryptError::CipherFailed`
- edge-cases HIGH #14: gap in middle of multi-chunk stream → add `gap_in_middle_fails_decryption()` test

**Tests to add (3):**
- `hkdf_uses_versioned_salt()`
- `encrypt_returns_error_not_panic_on_oversized()`
- `gap_in_middle_fails_decryption()`

---

### Worker 2.5 — P2P discovery hardening + TLS handshake timeout
**Files (exclusive):**
- `crates/copypaste-p2p/src/discovery.rs`
- `crates/copypaste-p2p/src/transport.rs`

**Findings to fix:**
- best-prac HIGH #6: 9× `Mutex::lock().unwrap()` in callback paths → switch to `parking_lot::Mutex` (workspace-already-present) OR `lock().unwrap_or_else(|e| e.into_inner())`
- edge-cases HIGH #13: TLS handshake no timeout → wrap `connect()`/`accept()` in `tokio::time::timeout(Duration::from_secs(10), ...)`
- edge-cases HIGH #12: rogue mDNS announce → ensure `TlsVerifier::verify_fingerprint` rejects with `UnknownPeer` (test only — verifier already correct)
- best-prac MEDIUM #16: stray `println!("Found peers during integration test")` in production path → `tracing::debug!` and gate behind `#[cfg(test)]`

**Tests to add (3):**
- `discovery_mutex_survives_callback_panic()`
- `tls_handshake_timeout_after_10s()`
- `rogue_mdns_peer_rejected_by_verifier()`

---

### Worker 2.6 — Daemon init/error-handling panic removal
**Files (exclusive):**
- `crates/copypaste-daemon/src/paths.rs`
- `crates/copypaste-daemon/src/launchd.rs`
- `crates/copypaste-daemon/src/tray.rs`
- `crates/copypaste-daemon/src/logging.rs`
- `crates/copypaste-daemon/src/main.rs`

**Findings to fix:**
- best-prac HIGH #1: `dirs::home_dir().expect("HOME directory must exist")` in `paths.rs` → return `Result<PathBuf>`
- best-prac HIGH #2: same in `launchd.rs` → return `Result`
- best-prac HIGH #3: 7× `menu.append(...).unwrap()` in `tray.rs::build_tray_menu` → use `?` + `TrayInitError`; log + continue without tray
- best-prac HIGH #4: `.build().expect("failed to create tray icon")` → graceful headless degradation
- best-prac HIGH #5: log appender panic → drop file layer, continue with stderr
- best-prac MEDIUM #21: `let _ = tokio::time::timeout(...)` in main.rs:84 → wrap with `if let Err(_) = ... { tracing::warn!("daemon did not stop within 3s") }`

**Tests to add (2):**
- `paths_returns_error_when_home_unset()`
- `tray_init_failure_logs_does_not_panic()`

---

### Worker 2.7 — Supabase realtime + push reliability
**Files (exclusive):**
- `crates/copypaste-supabase/src/realtime.rs`
- `crates/copypaste-daemon/src/cloud.rs` (small additions; coordinate ordering with Wave 1.6 — that lands first)

**Findings to fix:**
- security LOW #17: realtime.rs `warn!(payload = %msg.payload, ...)` leaks plaintext payload → redact (log length + first 16 bytes hex)
- edge-cases MEDIUM #19: WS disconnect mid-push → assert queued items flush on reconnect (existing backoff exists; need integration test)
- edge-cases MEDIUM #20: auth token expiry race (between request build and send) → add retry-on-401-with-refresh path
- edge-cases MEDIUM #21: 429 Retry-After header not honored → add exponential backoff that respects `Retry-After`

**Tests to add (4):** payload-redaction test, push-during-disconnect retries, token-expiry-race retry, 429-retry-after honored.

---

### Worker 2.8 — Sensitive false-positive corpus + Unicode bypass
**Files (exclusive):**
- `crates/copypaste-core/src/sensitive/detector.rs`
- `crates/copypaste-core/tests/false_positive_corpus.rs` (new)

**Findings to fix:**
- edge-cases HIGH #15: `generic_password_kv` matches benign forum posts → build 50+ benign corpus, baseline FP rate ≤5%
- edge-cases MEDIUM #28: Unicode-encoded secrets (full-width `ＡＫＩＡ`, ZWJ in JWT) → NFKC-normalise input before detect

**Tests to add (2):**
- `false_positive_rate_below_5pct_on_benign_corpus()`
- `nfkc_normalised_input_detects_secrets()`

---

### Worker 2.9 — CLI duplication cleanup + Android version pin + Cargo hygiene
**Files (exclusive):**
- `crates/copypaste-cli/src/commands/{export,list,clear,delete,private,status,count,copy}.rs`
- `crates/copypaste-cli/src/commands/common.rs` (new)
- `crates/copypaste-cli/Cargo.toml`
- `crates/copypaste-android/Cargo.toml`
- Workspace `Cargo.toml` (workspace.package additions)

**Findings to fix:**
- best-prac MEDIUM #9: extract `commands::common::exit_on_err(&resp)` → call from 8 files
- best-prac MEDIUM #20: `copypaste-android` hard-coded `version = "0.1.0"` → `version.workspace = true`
- best-prac MEDIUM #19: per-crate Cargo missing `license`, `description`, `repository` → add `workspace.package` defaults + `license.workspace = true` per crate
- architecture CRITICAL #3 (CLI-scope only): remove unused `copypaste-core = { path = ... }` dep from `copypaste-cli/Cargo.toml`

**Tests to add:** none (refactor + metadata only); CI must still build.

---

## Wave 3 — MEDIUM cleanups (run all 7 in parallel after Wave 2 verifies)

### Worker 3.1 — UI cold-boot empty-state + virtual scrolling guard
**Files (exclusive):**
- `crates/copypaste-ui/src/main.rs`
- `crates/copypaste-ui/src/ipc_client.rs`
- `crates/copypaste-ui/src/windows/HistoryWindow.slint` (or equivalent .slint files)

**Findings to fix:**
- edge-cases MEDIUM #25: `HistoryWindow` opened before daemon socket exists → show empty-state UI not crash
- edge-cases MEDIUM #26: 10k+ items in `ListView` no virtual scrolling → cap at server-side `MAX_PAGE` from Wave 2.3; document
- edge-cases MEDIUM #27: `PairWindow` no peers discovered → actionable empty state with troubleshooting hint
- best-prac LOW #28: `crates/copypaste-ui/src/lib.rs` `// lib.rs — ...` → `//! Slint UI windows for CopyPaste...`

**Tests to add (3):** cold-boot empty state, pair-window empty state, ui-list pagination respect.

---

### Worker 3.2 — Lifecycle / launchd recovery tests
**Files (exclusive):**
- `crates/copypaste-daemon/tests/lifecycle.rs` (new)

**Findings to fix (test-only):**
- edge-cases LOW #29: SIGKILL → launchd respawn → Lamport clock persisted
- edge-cases LOW #30: SIGTERM mid-sync → no half-written items
- edge-cases LOW #31: system sleep/wake → reconnect WS

**Tests to add (3):** sigkill_recovers_lamport, sigterm_no_half_written, wake_from_sleep_reconnects (stub test marked `#[ignore]` if env requires).

---

### Worker 3.3 — Storage DB corruption + key rotation API
**Files (exclusive):**
- `crates/copypaste-core/src/storage/db.rs` (rekey API only)
- `crates/copypaste-core/tests/corruption.rs` (new)

**Findings to fix:**
- edge-cases LOW #35: DB corruption mid-write → truncate `-wal`, assert `DbError::Corrupt` not silent data loss
- architecture MEDIUM #16: no `Database::rekey(&mut self, new_key: &[u8;32])` API → add method, issue `PRAGMA rekey = "x'...'"`, test

**Tests to add (2):** corrupted_wal_returns_corrupt_error, rekey_changes_key_and_data_still_readable.

---

### Worker 3.4 — Relay state RwLock + per-device limit on registration
**Files (exclusive):**
- `crates/copypaste-relay/src/state.rs` (RwLock conversion only; coordinate with Wave 1.2 which already touched this file → Wave 3.4 lands AFTER 1.2 verifies)
- `crates/copypaste-relay/src/routes/devices.rs` (per-device limit; same coordination)

**Findings to fix:**
- architecture MEDIUM #17: `Arc<Mutex<RelayStore>>` → `Arc<RwLock<RelayStore>>`; mark read-only handlers (`get_device`, `pull_items`, `list_devices`, `stats`) to use `.read()`
- security MEDIUM #13: per-device limit on `POST /devices` (in addition to per-IP)

**Tests to add (2):** read_only_handlers_use_rwlock_read, register_device_per_device_rate_limited.

**SEQUENCING WARNING:** Wave 3.4 touches the same files as Wave 1.2. Run 3.4 strictly AFTER 1.2 verifies and merges to release branch. If 1.2 is on a feature branch, rebase 3.4 onto it.

---

### Worker 3.5 — Daemon device_id persistence + Lamport persistence sanity
**Files (exclusive):**
- `crates/copypaste-daemon/src/daemon.rs` (device_id persistence only — coordinate with Wave 1.6 which also touched daemon.rs key-loading; sequence 3.5 AFTER 1.6)
- `crates/copypaste-daemon/src/paths.rs` (small helper; Wave 2.6 touched this file → sequence 3.5 AFTER 2.6)

**Findings to fix:**
- architecture LOW #24: `device_id` regenerated on every start → persist to `app_support_dir().join("device_id")`
- architecture LOW #22: cloud.rs `oneshot::Sender<()>` wrapped in `Arc<Notify>` workaround → replace with `tokio::sync::watch::channel(false)`

**Tests to add (1):** device_id_persists_across_restart.

**SEQUENCING WARNING:** Depends on Wave 1.6 + Wave 2.6. Schedule last in Wave 3 OR run as a serial follow-up.

---

### Worker 3.6 — IPC protocol error codes + structured logging tidy
**Files (exclusive):**
- `crates/copypaste-daemon/src/protocol.rs`
- `crates/copypaste-daemon/src/ipc.rs` (small additive change — touches same file as Wave 1.1; sequence AFTER 1.1 verifies)

**Findings to fix:**
- architecture MEDIUM #18: add `error_code: Option<&'static str>` alongside `error: Option<String>` (`not_found`, `auth_failed`, `invalid_argument`, `not_implemented`, `ipc_not_ready`); document in `docs/protocol.md`
- best-prac MEDIUM #8: extract `Response::not_implemented(req.id, feature_name)` helper; route the 6 cloud stubs through it
- best-prac MEDIUM #10: `format_fingerprint` triplicated → move to `copypaste-core::fingerprint`, re-export from UI/daemon (cross-crate refactor; small enough to fit one worker)

**Tests to add (1):** ipc_responses_carry_machine_readable_error_code.

**SEQUENCING WARNING:** Touches ipc.rs (W1.1) and adds a dep on copypaste-core from copypaste-ui (which Architecture CRITICAL #2 deferred). Limit refactor to the helper module only; do not change ui's existing types.

---

### Worker 3.7 — Documentation + README caveats (no code)
**Files (exclusive):**
- `README.md`
- `CHANGELOG.md`
- `docs/known-issues.md` (new)
- `docs/architectural-debt.md` (new — deferred-list register)

**Findings to fix (documentation only):**
- architecture CRITICAL #1: README note "P2P, cloud-sync, sync, supabase crates are scaffolding; not wired into the daemon for alpha"
- architecture CRITICAL #2: CHANGELOG entry "IPC protocol is unversioned; CLI/UI/daemon must be built from the same commit"
- architecture CRITICAL #4: README note "Relay is in-memory only; restart loses devices and sync items"
- best-prac LOW #25, #26, #27: track TODOs as GitHub issues; reference issue numbers
- architecture all remaining HIGH/MEDIUM not assigned to workers → register in `architectural-debt.md`

**No tests** (docs only).

---

## DEFERRED to post-alpha (architectural debt — `docs/architectural-debt.md`)

| # | Source | Finding | Reason for deferral |
|---|--------|---------|----------------------|
| arch-1 | architecture CRITICAL #1 | Integrate or remove orphan crates (`copypaste-p2p`, `-sync`, `-supabase`) | Multi-week refactor; documented in README for alpha |
| arch-2 | architecture CRITICAL #2 | Extract `copypaste-ipc` crate | Multi-week refactor; type drift acceptable for matched-commit alpha builds |
| arch-3 | architecture CRITICAL #4 | Relay persistence (`db.rs` either wire or delete) | Decided: delete `db.rs` is post-alpha hygiene; in-memory documented |
| arch-4 | architecture HIGH #5/#6 | `Arc<Mutex<Database>>` → connection pool + `spawn_blocking` | Invasive; needs design discussion + benchmarks |
| arch-5 | architecture HIGH #7 | Single `AppConfig` merge (core::config + daemon::ipc) | Migration tooling needed |
| arch-6 | architecture HIGH #8 | IPC protocol versioning | Needs ADR; document caveat in CHANGELOG for alpha |
| arch-7 | architecture HIGH #9/#10 | Platform abstraction either-or (delete or wire) | Phase 5b scope |
| arch-8 | architecture HIGH #11 | Workspace dedup (reqwest 0.11→0.12, rustls 0.21→0.23, hyper 0→1) | Major upgrade; coordinated multi-crate work |
| arch-9 | architecture MEDIUM #12 | Relay depend on `copypaste-sync` for `WireItem` | Tied to arch-1 |
| arch-10 | architecture MEDIUM #14 | Metrics export (Prometheus / OTLP) | Phase 2 |
| arch-11 | architecture MEDIUM #19 | Path resolver unification (`copypaste-paths` crate) | Cross-cutting; Phase 5b |
| arch-12 | architecture MEDIUM #20 | Hot-reload config | Document "restart required" instead |
| sec-x  | security MEDIUM #12 | SQLCipher migration probe side-channel | Low-risk; needs marker-file design |
| edge-x | edge-cases LOW #32 | Credit-card multi-line regex | New feature, not regression |
| edge-x | edge-cases LOW #33 | XChaCha doctest nonce-size | Documentation polish |
| edge-x | edge-cases INFO #36/#37/#38 | macOS image fixture, Windows placeholder, Android tests | Android `0 tests` accepted with "preview only" tag |
| bp-x   | best-prac MEDIUM #13/#14 | Modularise `ipc.rs` 931 LOC + 4 other oversize files | Post-alpha refactor |
| bp-x   | best-prac MEDIUM #15 | More `.context(...)` calls | Polish; not blocking |
| bp-x   | best-prac LOW #22/#23/#24 | Idiomatic `#[allow(clippy::unwrap_used)]`, BE/AE spelling | Cosmetic |

---

## File-conflict matrix (parallel-safety check)

| File | Wave 1 worker | Wave 2 worker | Wave 3 worker | Sequencing |
|------|---------------|----------------|----------------|------------|
| `daemon/src/ipc.rs` | 1.1 | 2.3 | 3.6 | strict sequential 1.1 → 2.3 → 3.6 |
| `daemon/src/daemon.rs` | 1.1 | — | 3.5 | 1.1 → 3.5 |
| `daemon/src/cloud.rs` | 1.6 | 2.7 | 3.5 | 1.6 → 2.7 → 3.5 |
| `daemon/src/keychain.rs` | 1.3 | — | — | safe |
| `daemon/src/p2p.rs` | 1.3 | — | — | safe |
| `daemon/src/clipboard.rs` | — | 2.1 | — | safe |
| `daemon/src/paths.rs` | — | 2.6 | 3.5 | 2.6 → 3.5 |
| `daemon/src/launchd.rs` | — | 2.6 | — | safe |
| `daemon/src/tray.rs` | — | 2.6 | — | safe |
| `daemon/src/logging.rs` | — | 2.6 | — | safe |
| `daemon/src/main.rs` | — | 2.6 | — | safe |
| `daemon/src/protocol.rs` | — | — | 3.6 | safe |
| `daemon/tests/integration_ipc.rs` | — | 2.3 | — | safe |
| `core/src/storage/schema.rs` | 1.5 | — | — | safe |
| `core/src/storage/db.rs` | 1.5 | — | 3.3 | 1.5 → 3.3 |
| `core/src/crypto/{encrypt,chunks,keys}.rs` | — | 2.4 | — | safe |
| `core/src/sensitive/detector.rs` | — | 2.8 | — | safe |
| `core/src/image.rs` | — | 2.2 | — | safe |
| `core/tests/concurrent_writers.rs` (new) | 1.5 | — | — | safe |
| `core/tests/false_positive_corpus.rs` (new) | — | 2.8 | — | safe |
| `core/tests/corruption.rs` (new) | — | — | 3.3 | safe |
| `sync/src/clock.rs` | 1.4 | — | — | safe |
| `sync/src/engine.rs` | 1.4 | — | — | safe |
| `p2p/src/discovery.rs` | — | 2.5 | — | safe |
| `p2p/src/transport.rs` | — | 2.5 | — | safe |
| `relay/src/state.rs` | 1.2 | — | 3.4 | 1.2 → 3.4 |
| `relay/src/routes/devices.rs` | 1.2 | — | 3.4 | 1.2 → 3.4 |
| `relay/src/routes/mod.rs` | 1.2 | — | — | safe |
| `supabase/src/realtime.rs` | — | 2.7 | — | safe |
| `cli/src/commands/*.rs` | — | 2.9 | — | safe |
| `cli/Cargo.toml` | — | 2.9 | — | safe |
| `android/Cargo.toml` | — | 2.9 | — | safe |
| `Cargo.toml` (workspace) | — | 2.2 + 2.9 | — | **COORDINATE 2.2 & 2.9** (both edit workspace.dependencies + workspace.package — different sections, but same file; serialise 2.2 → 2.9 or merge manually) |
| `ui/src/*` | — | — | 3.1 | safe |
| `README.md`, `CHANGELOG.md`, `docs/*` | — | — | 3.7 | safe |

**Conflict resolution rule:** when two workers target the same file across waves, the later wave waits for the earlier wave's PR to land on `release/v0.1.0-alpha` first. Within the same wave, no two workers share a file (verified above).

**Wave 2 internal coordination:** 2.2 and 2.9 both edit root `Cargo.toml` but in different sections (`[workspace.dependencies]` vs `[workspace.package]`). Run 2.2 first; 2.9 rebases.

---

## Execution order (orchestrator playbook)

1. **Spawn all 6 Wave-1 workers in ONE message** (parallel, `run_in_background: true`). Workers must call `task-queue.sh claim` as FIRST command and `complete` as LAST.
2. After ALL Wave 1 workers complete:
   - Run `cargo test --workspace --all-features` from release-alpha worktree
   - Run `cargo clippy --workspace -- -D warnings`
   - If green: merge each worker's branch into `release/v0.1.0-alpha`
3. **Spawn all 9 Wave-2 workers in ONE message** (parallel), respecting internal sequencing:
   - 2.2 runs first (alone) → cargo verify → then spawn 2.1, 2.3-2.9 in parallel
   - OR spawn all 9 simultaneously and let 2.9 rebase on top of 2.2 manually
4. After Wave 2 verifies: spawn Wave 3 (7 workers in parallel). Workers 3.4, 3.5, 3.6 explicitly wait on Wave 1 + 2 files they share.
5. After ALL waves complete:
   - Re-run security + edge-cases audits (delta against original)
   - Run Phase A end-to-end smoke test
   - Tag `v0.1.0-alpha.1`

---

## Coverage check

| Audit                | Total findings | Fixed in plan | Deferred (architectural-debt.md) | Skipped (low/info, accepted) |
|---------------------|----------------|----------------|-----------------------------------|------------------------------|
| security            | 21             | 17             | 1 (#12)                           | 3 (#21 partial, #22, #18 ride-along)|
| edge-cases          | 38             | 21             | 6                                 | 11 (low/info)                |
| best-practices      | 28             | 14             | 5                                 | 9 (low/info docs)            |
| architecture        | 27             | 5              | 12                                | 10 (info or duplicate)       |
| **TOTAL**           | **114**        | **57** (35 unique work-items grouped by file scope) | **24** | **33** |

The plan's "35 findings to fix" cap is the unique fix-units distributed across 22 workers (Wave 1: 6, Wave 2: 9, Wave 3: 7). Several workers bundle 2-6 closely-related findings on the same file scope, so total audit findings closed is 57.

---

## Return summary
- **Total waves designed:** 3
- **Total workers:** 22 (6 + 9 + 7)
- **Total audit findings covered (closed):** 57 / 114
- **Total findings deferred (architectural-debt.md):** 24
- **Estimated parallel wall-time:** ~3 hours (1h per wave + cargo verify between waves)
