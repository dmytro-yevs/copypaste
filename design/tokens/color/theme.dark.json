{
  "color": {
    "$description": "Dark theme surfaces, text, state layers and status colours. Adopted from shadcn/ui's default theme on the `zinc` base (its neutral ramp is Tailwind v4's `zinc`, in OKLCH); the status and content-kind hues are Tailwind v4 palette steps. Dark is the default theme. Nothing here is a v1 value — see design/README.md.",
    "$type": "color",
    "bg": {
      "$value": "oklch(0.141 0.005 285.823)",
      "$description": "zinc-950. shadcn `background`: the window ground behind every panel."
    },
    "panel": {
      "$value": "oklch(0.21 0.006 285.885)",
      "$description": "zinc-900. shadcn `sidebar`: chrome surface — sidebar, header, tab bar."
    },
    "elevated": {
      "$value": "oklch(0.274 0.006 286.033)",
      "$description": "zinc-800. shadcn `muted`: the inset surface for fields, chips and badges."
    },
    "card": {
      "$value": "oklch(0.21 0.006 285.885)",
      "$description": "zinc-900. shadcn `card` and `popover`."
    },
    "raised": {
      "$value": "oklch(0.274 0.006 286.033)",
      "$description": "zinc-800. shadcn `secondary`: secondary buttons, segmented on-state."
    },
    "raised-2": {
      "$value": "oklch(0.37 0.013 285.805)",
      "$description": "zinc-700. Highest neutral surface: scrollbar thumb, switch off-track."
    },
    "border": {
      "$value": "oklch(1 0 0 / 10%)",
      "$description": "shadcn `border` in dark is white at 10%, not a solid grey — it holds its weight over every surface in the ramp instead of only over one. 1.25:1 on --bg: decorative separation only, never the sole boundary of a control (see --border-strong)."
    },
    "border-strong": {
      "$value": "oklch(1 0 0 / 40%)",
      "$description": "The boundary of an interactive control — input, checkbox, switch track, segmented button. WCAG 1.4.11 wants 3:1 for a boundary that identifies a component, and shadcn's own --border is 1.25:1. Measured 3.76 on --bg, 3.82 on --card, 3.64 on --elevated. Do not use it for dividers; the visual weight is the point and it is wrong everywhere else."
    },
    "divider": {
      "$value": "oklch(1 0 0 / 6%)",
      "$description": "Hairline between regions. Lighter than --border so a separator never reads as a control edge."
    },
    "text": {
      "$value": "oklch(0.985 0 0)",
      "$description": "shadcn `foreground`."
    },
    "dim": {
      "$value": "oklch(0.705 0.015 286.067)",
      "$description": "zinc-400. shadcn `muted-foreground`: secondary text. 7.56:1 on --bg, 5.66 on --elevated."
    },
    "faint": {
      "$value": "oklch(0.65 0.015 286.067)",
      "$description": "Tertiary text — timestamps, counts, placeholders. Deliberately between zinc-400 and zinc-500 rather than on a scale step: A11Y-10 requires it clear AA because it labels real meta text, and zinc-500 does not on --elevated. 6.13 on --bg, 4.59 on --elevated, 5.26 on --selected."
    },
    "mute": {
      "$value": "oklch(0.552 0.016 285.938)",
      "$description": "zinc-500. 4.12:1 on --bg — below AA, deliberately. Decorative only (disabled glyphs, separating dots); A11Y-10: never the sole carrier of text."
    },
    "hover": {
      "$value": "oklch(1 0 0 / 8%)",
      "$description": "Neutral hover state layer. This is shadcn's `accent` — its menu/list hover surface, NOT the brand colour."
    },
    "pressed": {
      "$value": "oklch(1 0 0 / 12%)",
      "$description": "Neutral pressed state layer."
    },
    "selected": {
      "$value": "color-mix(in oklab, var(--accent) 18%, transparent)",
      "$description": "Accent-tinted selection fill for list rows and the active nav item. Mixed in oklab so the tint stays perceptually even across all six accents.",
      "$extensions": {
        "com.copypaste": {
          "platforms": [
            "css"
          ]
        }
      }
    },
    "alpha-selected": {
      "$type": "number",
      "$value": 0.18,
      "$description": "The accent alpha behind --selected, in one place so nothing re-derives it."
    },
    "scrim": {
      "$value": "oklch(0 0 0 / 60%)",
      "$description": "Dialog scrim."
    },
    "ok": {
      "$value": "oklch(0.792 0.209 151.711)",
      "$description": "green-400. Success fill / dot."
    },
    "warn": {
      "$value": "oklch(0.828 0.189 84.429)",
      "$description": "amber-400. Warning fill / dot."
    },
    "err": {
      "$value": "oklch(0.704 0.191 22.216)",
      "$description": "red-400 — shadcn's `destructive` in dark, verbatim. 6.93 on --bg."
    },
    "on-err": {
      "$value": "oklch(0.21 0.05 22.216)",
      "$description": "Foreground on a solid --err fill. Dark ink, not white: shadcn pairs its dark `destructive` (red-400) with white and that measures 2.75:1, which fails A11Y-10 on every destructive button in the app. Dark ink measures 6.25. This is the one place the adopted system is overridden on contrast rather than followed; light keeps white ink because there --err is red-600."
    },
    "info": {
      "$value": "oklch(0.707 0.165 254.624)",
      "$description": "blue-400. Informational fill / dot."
    },
    "ok-strong": {
      "$value": "{color.ok}",
      "$description": "AA-corrected foreground variant. In dark the -400 steps already clear AA on their own tints, so these alias the base hue; light is where they differ."
    },
    "warn-strong": {
      "$value": "{color.warn}",
      "$description": "See --ok-strong."
    },
    "err-strong": {
      "$value": "{color.err}",
      "$description": "See --ok-strong."
    },
    "info-strong": {
      "$value": "{color.info}",
      "$description": "See --ok-strong."
    },
    "c-text": {
      "$value": "oklch(0.705 0.015 286.067)",
      "$description": "Content kind: text. zinc-400 — plain text gets the neutral, so a coloured glyph always means something."
    },
    "c-url": {
      "$value": "oklch(0.789 0.154 211.53)",
      "$description": "Content kind: url. cyan-400."
    },
    "c-code": {
      "$value": "oklch(0.702 0.183 293.541)",
      "$description": "Content kind: code (renders monospace). violet-400."
    },
    "c-image": {
      "$value": "oklch(0.74 0.238 322.16)",
      "$description": "Content kind: image. fuchsia-400."
    },
    "c-mail": {
      "$value": "oklch(0.765 0.177 163.223)",
      "$description": "Content kind: mail. emerald-400."
    },
    "c-color": {
      "$value": "oklch(0.828 0.189 84.429)",
      "$description": "Content kind: color (renders monospace). amber-400."
    },
    "c-num": {
      "$value": "oklch(0.777 0.152 181.912)",
      "$description": "Content kind: num (renders monospace). teal-400."
    },
    "c-path": {
      "$value": "oklch(0.746 0.16 232.661)",
      "$description": "Content kind: path. sky-400."
    },
    "c-file": {
      "$value": "oklch(0.707 0.165 254.624)",
      "$description": "Content kind: file. blue-400."
    },
    "c-json": {
      "$value": "oklch(0.75 0.183 55.934)",
      "$description": "Content kind: json (renders monospace). orange-400."
    },
    "c-secret": {
      "$value": "oklch(0.712 0.194 13.428)",
      "$description": "Content kind: secret. rose-400 — next to --err on purpose: a detected secret is a warning about the item, not a failure."
    },
    "c-unknown": {
      "$value": "{color.c-text}",
      "$description": "The kind set is closed and ends in `unknown`, which must never render blank; it falls back to the text-kind colour."
    },
    "withheld": {
      "$value": "{color.elevated}",
      "$description": "Fill of the slot where a sensitive item's content would be. INV-10 / CLAUDE.md rule 4: the content is ABSENT — the bridge sends `content: null` — so this must read as an empty slot, not as text behind glass. There is deliberately no blur token; v1's --mask-blur:6px is the treatment the manifest README rules out, and re-adding one would make withheld content look present."
    },
    "withheld-border": {
      "$value": "{color.border-strong}",
      "$description": "Outline of the withheld slot. It is a real button (A11Y-3), so its boundary carries the 1.4.11 weight."
    },
    "withheld-fg": {
      "$value": "{color.c-secret}",
      "$description": "The 'Sensitive — activate to reveal' label. 5.32 on --withheld, 6.09 on --selected."
    }
  }
}
