{
  "motion": {
    "$description": "Motion: Tailwind v4's default transition duration and its ease-out curve, which is what shadcn's own animations are timed against. All three durations collapse to 0ms under prefers-reduced-motion (A11Y-11); that override is emitted into the generated CSS from the reducedMotion extension below, so the accessibility rule lives with the token rather than in a stylesheet someone has to remember.",
    "dur-fast": {
      "$type": "duration",
      "$value": "150ms",
      "$description": "Tailwind's default transition duration. Hover, press, colour.",
      "$extensions": {
        "com.copypaste": {
          "reducedMotion": "0ms"
        }
      }
    },
    "dur": {
      "$type": "duration",
      "$value": "200ms",
      "$description": "Enter/exit of an overlay — dialog, popover, toast.",
      "$extensions": {
        "com.copypaste": {
          "reducedMotion": "0ms"
        }
      }
    },
    "dur-theme": {
      "$type": "duration",
      "$value": "300ms",
      "$description": "Theme cross-fade.",
      "$extensions": {
        "com.copypaste": {
          "reducedMotion": "0ms"
        }
      }
    },
    "ease": {
      "$type": "cubicBezier",
      "$value": [
        0,
        0,
        0.2,
        1
      ],
      "$description": "Tailwind's `ease-out`. One curve for the whole app: UI motion should decelerate into place and never ease in."
    }
  }
}
