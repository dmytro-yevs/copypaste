/*
 * CopyPaste Material 3 ColorSchemes.
 *
 * GENERATED by Style Dictionary from design/tokens/. DO NOT EDIT.
 * Change a token and run `npm run build` in design/.
 */

package app.copypaste.design

import androidx.compose.material3.ColorScheme
import androidx.compose.material3.darkColorScheme
import androidx.compose.material3.lightColorScheme
import androidx.compose.runtime.staticCompositionLocalOf

/**
 * The CopyPaste dark theme as a Material 3 ColorScheme. Every stock Material
 * component is themed by this — see the mapping table in design/README.md for
 * which CopyPaste token feeds each role.
 */
public fun copyPasteDarkColorScheme(
  accent: CopyPasteAccent = CopyPasteAccent.Indigo,
): ColorScheme {
  val c = copyPasteDarkColors
  val a = copyPasteDarkAccents.getValue(accent)
  return darkColorScheme(
    primary = a.accent,
    onPrimary = a.onAccent,
    primaryContainer = a.accent2,
    onPrimaryContainer = c.onAccent2,
    inversePrimary = a.accent2,
    secondary = c.raised,
    onSecondary = c.text,
    secondaryContainer = a.accent.copy(alpha = c.alphaSelected),
    onSecondaryContainer = c.text,
    tertiary = a.accent2,
    onTertiary = c.onAccent2,
    tertiaryContainer = c.raised2,
    onTertiaryContainer = c.text,
    background = c.bg,
    onBackground = c.text,
    surface = c.panel,
    onSurface = c.text,
    surfaceVariant = c.raised,
    onSurfaceVariant = c.dim,
    surfaceTint = a.accent,
    inverseSurface = c.text,
    inverseOnSurface = c.bg,
    error = c.err,
    onError = c.onErr,
    errorContainer = c.err.copy(alpha = c.alphaTint),
    onErrorContainer = c.errStrong,
    outline = c.border,
    outlineVariant = c.divider,
    scrim = c.scrim,
    surfaceDim = c.bg,
    surfaceBright = c.raised2,
    surfaceContainerLowest = c.bg,
    surfaceContainerLow = c.panel,
    surfaceContainer = c.elevated,
    surfaceContainerHigh = c.raised,
    surfaceContainerHighest = c.raised2,
  )
}

/**
 * The CopyPaste light theme as a Material 3 ColorScheme. Every stock Material
 * component is themed by this — see the mapping table in design/README.md for
 * which CopyPaste token feeds each role.
 */
public fun copyPasteLightColorScheme(
  accent: CopyPasteAccent = CopyPasteAccent.Indigo,
): ColorScheme {
  val c = copyPasteLightColors
  val a = copyPasteLightAccents.getValue(accent)
  return lightColorScheme(
    primary = a.accent,
    onPrimary = a.onAccent,
    primaryContainer = a.accent2,
    onPrimaryContainer = c.onAccent2,
    inversePrimary = a.accent2,
    secondary = c.raised,
    onSecondary = c.text,
    secondaryContainer = a.accent.copy(alpha = c.alphaSelected),
    onSecondaryContainer = c.text,
    tertiary = a.accent2,
    onTertiary = c.onAccent2,
    tertiaryContainer = c.raised2,
    onTertiaryContainer = c.text,
    background = c.bg,
    onBackground = c.text,
    surface = c.panel,
    onSurface = c.text,
    surfaceVariant = c.raised,
    onSurfaceVariant = c.dim,
    surfaceTint = a.accent,
    inverseSurface = c.text,
    inverseOnSurface = c.bg,
    error = c.err,
    onError = c.onErr,
    errorContainer = c.err.copy(alpha = c.alphaTint),
    onErrorContainer = c.errStrong,
    outline = c.border,
    outlineVariant = c.divider,
    scrim = c.scrim,
    surfaceDim = c.bg,
    surfaceBright = c.raised2,
    surfaceContainerLowest = c.bg,
    surfaceContainerLow = c.panel,
    surfaceContainer = c.elevated,
    surfaceContainerHigh = c.raised,
    surfaceContainerHighest = c.raised2,
  )
}

/**
 * The CopyPaste tokens Material 3 has no role for — the content-kind hues
 * (§8.7), the AA-corrected *Strong text variants (§8.3) and the state layers.
 * Provide it alongside MaterialTheme; read it for anything the ColorScheme
 * cannot express.
 */
public val LocalCopyPasteColors: androidx.compose.runtime.ProvidableCompositionLocal<CopyPasteColors> =
  staticCompositionLocalOf { copyPasteDarkColors }
