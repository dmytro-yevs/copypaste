{
  "name": "@copypaste/design-tokens",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "The single source of truth for CopyPaste colour, type, spacing and motion. Compiled by Style Dictionary to CSS custom properties (macOS/Tauri desktop), a Tailwind v4 @theme layer (shadcn/ui), and Kotlin/Compose Material 3 (Android).",
  "license": "MIT OR Apache-2.0",
  "files": [
    "tokens",
    "dist",
    "style-dictionary.config.js"
  ],
  "exports": {
    "./css": "./dist/css/index.css",
    "./css/tokens": "./dist/css/tokens.base.css",
    "./css/theme": "./dist/css/theme.css",
    "./tokens/*": "./tokens/*"
  },
  "scripts": {
    "build": "node style-dictionary.config.js",
    "clean": "node -e \"import('node:fs').then(fs=>fs.rmSync('dist',{recursive:true,force:true}))\"",
    "rebuild": "npm run clean && npm run build"
  },
  "devDependencies": {
    "style-dictionary": "^5.5.0"
  }
}
