//! Beta-bonus benchmark harness for CopyPaste.
//!
//! This crate is intentionally empty at the library level — all measurable
//! work lives in `benches/*.rs` and is run via `cargo bench -p copypaste-bench`.
//! See `benches/encryption.rs`, `benches/storage.rs`, `benches/hash.rs`.
