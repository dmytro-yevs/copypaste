<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.copypaste_ui" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!--
      IntakeActivity's theme. It exists only to hold window focus for the
      instant a clipboard read needs it, so it must not draw, must not animate,
      and must not dim what is behind it: the user asked to save a clip, not to
      leave the app they were in.
    -->
    <style name="Theme.CopyPaste.Invisible" parent="android:Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
