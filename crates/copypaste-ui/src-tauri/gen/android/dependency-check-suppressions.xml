<?xml version="1.0" encoding="UTF-8"?>
<!--
  Every rule names the artifact, why the advisory cannot reach the shipped APK,
  and what would make that wrong. A rule without the third part is a lie the
  next person inherits.

  failBuildOnUnusedSuppressionRule is on, so a rule that stops matching fails
  the build. That is the delete prompt: it fires on the upgrade, not on a date
  someone guessed.
-->
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.4.xsd">

    <suppress>
        <notes><![CDATA[
        androidx.browser:browser is the AndroidX Custom Tabs client. CVE-2008-7298
        is scored against cpe:2.3:a:android:android_browser, the AOSP stock Browser
        application that the platform dropped in Android 5.0, and describes that app
        letting a Set-Cookie over plain HTTP overwrite a cookie set in an HTTPS
        session. This library has no cookie store and no HTTP stack; it hands a URL
        to whichever browser the user installed, and that browser owns the jar. The
        match is on the token "browser" alone.

        Two report entries are the same module: the .aar and the classes.jar AGP
        transforms out of it. The packageUrl covers both.

        Wrong if: NVD widens CVE-2008-7298's CPE set to cover androidx.browser. Any
        other advisory against this artifact still fails the build - this rule names
        one CVE, not the dependency.
        ]]></notes>
        <packageUrl regex="true">^pkg:maven/androidx\.browser/browser@.*$</packageUrl>
        <cve>CVE-2008-7298</cve>
    </suppress>

    <suppress>
        <notes><![CDATA[
        androidx.exifinterface:exifinterface reads EXIF out of image files on
        Android. CVE-2009-1501 is a stored XSS in the Exif module for Drupal 5.x and
        6.x (cpe:2.3:a:exif:exif, cpe:2.3:a:drupal:drupal) - PHP that writes EXIF tag
        values into a web page. No shared code, no shared vendor, and no HTML sink in
        this artifact. The match is on the token "exif" alone.

        Wrong if: NVD widens CVE-2009-1501's CPE set to cover androidx.exifinterface.
        ]]></notes>
        <packageUrl regex="true">^pkg:maven/androidx\.exifinterface/exifinterface@.*$</packageUrl>
        <cve>CVE-2009-1501</cve>
    </suppress>

    <suppress>
        <notes><![CDATA[
        "Code execution was possible via unsafe deserialization in the build cache
        metadata" is Kotlin's build tooling. NVD scores it against
        cpe:2.3:a:jetbrains:kotlin - the whole product - so every artifact in the
        org.jetbrains.kotlin group below 2.4.20 inherits the 9.8, the standard
        library included.

        Checked, not assumed: kotlin-stdlib 2.2.21 holds 1037 entries, 1029 of them
        under kotlin/ and the remaining 8 META-INF metadata. The code that reads
        build cache metadata is in kotlin-compiler-embeddable and
        kotlin-build-tools-impl, which run on the build host and are not packaged
        into the APK.

        There is also nothing to upgrade to. On 2026-08-08 the newest kotlin-stdlib
        on Maven Central is 2.4.20-Beta2, so the fixed release named by the advisory
        has not shipped to anyone.

        Wrong if: the vulnerable path turns out to be reachable from the standard
        library itself. When Kotlin 2.4.20 ships and this project takes it, the rule
        stops matching and the build says so; delete it then.

        Not covered here: the build host's own Kotlin toolchain, which is genuinely
        affected and is outside this audit's scope by ADR-0016.
        ]]></notes>
        <packageUrl regex="true">^pkg:maven/org\.jetbrains\.kotlin/kotlin-stdlib(-jdk[78])?@.*$</packageUrl>
        <cve>CVE-2026-53914</cve>
    </suppress>

</suppressions>
