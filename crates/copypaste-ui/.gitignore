# Node / Vite
node_modules
dist
*.local

# Tauri
src-tauri/target
src-tauri/gen/schemas

# Bundled font binaries — committed (small subsets, ~24KB each) so the build is
# reproducible from a fresh clone (a missing woff2 404s → SPA fallback serves
# index.html → OTS "invalid sfntVersion"). Inter + JetBrains Mono, @font-face
# refs in src/index.css use ./assets/fonts/ (relative to src/, the file's dir).

e2e/dev-drive/__artifacts__/
