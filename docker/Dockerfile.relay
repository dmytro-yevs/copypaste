FROM rust:1.75-slim-bookworm AS builder
WORKDIR /workspace

FROM debian:bookworm-slim
RUN apt-get update && apt-get install -y ca-certificates && rm -rf /var/lib/apt/lists/*
EXPOSE 8080
CMD ["echo", "Relay server - build in Phase 2"]
