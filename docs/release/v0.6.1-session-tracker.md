# v0.6.1 — Live session tracker

Single source of truth for everything requested/found this session. Updated as work lands.
Integration branch: `v0.6.1-integration`.

## ✅ DONE — merged + verified on hardware (on devices now)
- **P2P sync both directions** (macOS↔Android) — 3 layered root causes fixed: write-timeout+TCP keepalive; mDNS IP-correlated port refresh (dial fresh port); push_catchup deadlock (spawn drainer before catch-up). + Android symmetric mDNS port refresh.
- **base64 WireItem** — binary fields were JSON number-arrays (3.5x bloat) → files >4.5MiB blew the 16MiB frame → file sync + large-item teardown. Now base64.
- **wall_time preserved on synced items** — was stamped receive-time → "just now" / wrong order / **sync thrash** (ping-pong). Fixed → LWW converges.
- **image-copy duplicate guard** (Android) — copying an image from the list no longer creates a duplicate.
- **Mac reactive peer name+addr** — refreshed live from mDNS.
- **macOS file upload + drag-drop + draggable window header** (built; lands at next checkpoint).
- **device name** — Mac advertises real ComputerName ("MacBook Air"), not "CopyPaste".

## 🔜 NEXT CHECKPOINT — branches ready/merging → rebuild APK+daemon → reinstall
- Android devices **table layout + single online source** (footer==card) + vertical-centered rows + live local IP
- macOS devices **table layout + single live online source** (daemon peer_sinks) + own-info polling (Public IP)
- **full history sync** — removed the 200-item cap; truncate only by byte-cap retention
- **background capture (ClipCascade technique)** — logcat-denial detect + transparent FOCUSABLE overlay Activity read (no a11y dependency; works on ColorOS)
- **deep list-perf** — memoize decrypt, stable items/lambdas, skip no-op emits (history jank)  ← in progress
- **macOS auto-apply** — received clip → pasteboard with self-write loop guard  ← in progress

## 🛠 QUEUED — designed, ready to implement (in order)
1. **Android auto-apply** received clip → system clipboard (expectClip loop guard). [pairs with macOS auto-apply]
2. **Preview long-press (peek/pop)** — hold = enlarged preview; drag up (no release) = pin; pinned = pinch-zoom image / scroll text. Long-press repurposed (checkbox keeps multi-select — user OK'd). Design: full state machine done.
3. **Sync ALL operations** (delete/pin/reorder/clear) via **soft-delete tombstone + LWW** — deletes currently resurrect (no tombstones anywhere). Plan: schema v10 + WireItem `deleted`/`pinned`/`pin_order` + broadcast on delete + Supabase column + UDL ABI bump (no re-pair).
4. **DeviceInfoAnnounce frame** — full device-info reactivity (model/OS/app_version/public_ip + authoritative name) over the live mTLS link (mDNS can't carry these). [batch ABI bump with #3 + #5]
5. **Origin-device per clip + filter + sort** (both platforms) — backend ~70% exists (`origin_device_id`); needs UUID→name map (`PairedDevice.device_id`), IPC passthrough, UI, Android blob v3, UDL field.
6. **Type chips: unify labels** (macOS shows "T", Android shows "TEXT" → full words everywhere) + **more type auto-detection** (URL/email/phone/code/color/JSON…) shown as subtype chip.
7. **Clip count** display (both platforms).
8. **Android image-height** — real size control (width-cap + 340px decode currently limit it).
9. **bg-capture onboarding UX** — request overlay via real "Display over other apps" intent + self-test (so no adb needed).

## 🔁 Discipline
read-only agents (audit/design) run freely in parallel; write agents serialized per shared file (1 Android-HistoryActivity writer, 1 DevicesActivity writer, etc.); main tree committed + HEAD restored after every spawn/merge; merges via merge-train (merger subagent), disjoint-first, BUILD-gated.

## Design docs (in ~/.claude memory)
reactivity audit · op-propagation (tombstone) · origin-device · ClipCascade bg-capture technique · list-perf root causes · preview UX spec.
