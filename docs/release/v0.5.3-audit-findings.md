# v0.5.3 Full-Codebase Audit & Per-Task Verification — Findings

> Generated 2026-06-02 by a fleet of read-only auditors (one per crate/area) + per-task
> acceptance verifiers. Severity: P0 critical / P1 important / P2 minor.

## STATUS LEGEND
- ✅ fixed **and committed** (Rust+UI wave `7944907`)
- 🟡 fixed in working tree, **commit pending** (Android wave, not yet compiled/committed)
- ⏳ in progress (agent running now)
- ☐ not yet started
- ⛔ deferred — Tier-4 architectural / ABI / protocol change, or explicitly out-of-scope

**Commits so far:** `60796d0` (HW-feedback backlog) · `7944907` (Tier-1/2/3 Rust+UI hardening,
cargo+clippy+tsc+vitest green) · `8a5d711` (Android wave, compileDebugKotlin green — all 6
Android agents: services, pairing, history+HW-A8, onboarding+HW-A11, main, logviewer/settings).
· `1e8a5e7` (remaining ☐ items: UI guards, daemon catchup/device-name, **cloud-sync P0/P1**
WS-JWT refresh + auto-refresh + 429 + realtime user_id filter, core zeroize/serde_json,
relay TOCTOU, ffi/telemetry, cli — default + `--features cloud-sync` + clippy + tsc + vitest green;
also fixes the **popup-shortcut-not-saved** user report: non-fatal startup register + resilient load).
**Remaining = ⛔ Tier-4 only** (need your decision). A read-only verification fleet is now
re-checking every session fix for correctness/integration/new-bugs.

---

## A. MODULE BUG AUDITS

### copypaste-ui (React/TS + src-tauri)  — committed `7944907`
- ✅ **P0** SyncStatusChip.tsx:98 — stale-closure/leak → wrapped in useCallback + cancelled guard.
- ✅ **P0** src-tauri/lib.rs:1488 — tray resync infinite loop → AtomicBool stop flag on RunEvent::Exit.
- ✅ **P0** SettingsView.tsx:795 — handleP2pToggle stale config → uses buildConfigPatch.
- ✅ **P0** SettingsView.tsx:940 — sound/notify toggles → guarded on loadState ready.
- ✅ **P1** Popup.tsx:167 — onFocusChanged teardown race → cancelled flag.
- ✅ **P1** DevicesView.tsx:325 — loadPeers stale ownState → ownFpRef.
- ✅ **P1** HistoryView.tsx:983 — auto-refresh double-load → loadStateRef, removed dep.
- ✅ **P1** SettingsView.tsx:809 — handleWifiOnlyToggle double-revert → onRevert callback.
- ☐ **P1** SettingsView.tsx:641 — load effect cancelled guard (NOT addressed; minor).
- ✅ **P1** LogView.tsx:40 — Object URL revoke → deferred setTimeout(1000).
- ✅ **P1** src-tauri/lib.rs:1235 — tray copy now fires sound/notification.
- ✅ **P1** ImageThumb.tsx:66 — in-flight dedup map + placeholder on error.
- ✅ **P2** store.ts:85 — bare catch now console.warn.
- ✅ **P2** src-tauri/ipc.rs:74 — IPC_REQUEST_ID const.
- ✅ **P2** src-tauri/lib.rs:1024/877 — fallback popup dims fixed (assertion not added).
- ☐ **P2** HistoryView.tsx:861 — module-level `_toastSeq` (NOT addressed).
- ☐ **P2** HistoryView.tsx:1041 — multi-select micro-task race (NOT addressed).
- ✅ **P2** Popup.tsx:182 — FOCUS_DELAY_MS const.
- ✅ **P2** DevicesView.tsx:403 — handleUnpair/Revoke cancelled guard.
- ✅ **P2** daemon_lifecycle.rs:514 — mutex poison now logged.
- ✅ **P2** lib.rs:285 — set_popup_shortcut unregister error logged.
- ✅ **P2** ipc.rs:190 — read_logs rotation behavior documented.
- ✅ **P2** SettingsView.tsx:696 — saveLimitsField per-field revert.

### copypaste-daemon  — committed `7944907`
- ✅ **P0** ipc.rs:595 — save_peers now atomic 0600 (no world-readable sync-key window).
- ✅ **P0** sync_orch.rs:427 — preserve wire.key_version on verbatim merge.
- ✅ **P0** sync_orch ingest — `wire.clamp_timestamps()` now called in merge_incoming_with_crypto.
- ✅ **P1** daemon.rs:1114 — afplay/osascript children reaped (no zombies).
- ✅ **P1** daemon.rs:747/809 — TTL intervals → named consts.
- ✅ **P1** cloud.rs:759/1572 — reqwest 30s timeout.
- ✅ **P1** ipc.rs:1837/2167 — bump_item_recency errors now logged.
- ☐ **P1** sync_orch.rs:458 — catchup_items 10k hardcode (NOT addressed; deferred).
- ☐ **P1** daemon.rs:508 / ipc.rs:3876 — device-name fallback dedup (NOT addressed; minor).
- ⛔ **P1** ipc.rs:3510/3780 — PAKE channel binding (Tier-4, deferred).
- ☐ **P2** daemon.rs:997 — run_degraded busy-poll (NOT addressed).
- ✅ **P2** daemon.rs:1209 — ITEM_KEY_VERSION_CURRENT_U32 uses core const + assert.
- ✅ **P2** daemon.rs:741 — sensitive_ttl==0 guard (Option, disables deletion).
- ☐ **P2** cloud.rs:784 — backlog LIMIT 1024 (NOT addressed).
- ☐ **P2** sync_orch.rs:87 — shared_sync_key per-item disk read (NOT addressed).

### copypaste-supabase (+ cloud.rs consumer)  — cloud-sync feature; partial
- ✅ **P0** cloud.rs:804 — non-text rows now drained (is_synced=1 + warn) so they stop re-queuing; images still not uploaded (encode path not implemented — documented).
- ☐ **P0** cloud.rs:1793 — WS reconnect stale JWT (NOT addressed; cloud-sync, important).
- ☐ **P1** auth.rs:220 — spawn_auto_refresh never called (NOT addressed).
- ☐ **P1** cloud.rs:2671 — poll 429 Retry-After (NOT addressed).
- ☐ **P1** realtime.rs:235 — realtime user_id filter (NOT addressed).
- ✅ **P1** cloud.rs:760/1572 — reqwest timeout (done with daemon Fix 6).
- ☐ **P2** misc supabase silent swallows / vsn / poll-limit literal (NOT addressed).

### copypaste-ipc + copypaste-cli  — committed `7944907`
- ✅ **P0** cli common.rs:28 — days_to_ymd upper-bound guard (no hang).
- ✅ **P1** cli ipc.rs:103/143 — id match via serde_json::Value.
- ✅ **P1** cli cloud.rs:125 — password plaintext exposure minimized.
- ✅ **P1** cli import.rs:29 — 64 MiB file-size cap.
- ✅ **P1** cli status.rs:164 — next_id() everywhere.
- ✅ **P2** vacuum.rs checkpoint error logged; watch.rs interval-0 guard; METHOD_* consts added.
- ☐ **P2** copy.rs process::exit propagation; export.rs TOCTOU; ipc Response type divergence (minor, NOT addressed).

### copypaste-android FFI + copypaste-telemetry  — committed `7944907` (partial)
- ✅ **P0** android/src/lib.rs — `add_clipboard_item` AAD now build_item_aad_v2(id,V4,2) (dual-sync breaker fixed; Kotlin DOES use this path).
- ✅ **P1** lib.rs:177 — passphrase char-count guard + explicit SyncKeyError arms.
- ✅ **P1** lib.rs:856 — with_cached_db no FFI-boundary panic.
- ⛔ **P1** lib.rs:600 — sync_with_peer random origin_device_id (needs UDL device_id param + ABI bump → Tier-4, deferred).
- ✅ **P2** lib.rs:488/659 — P2P salt compile-assert + comment; key_version named const.
- ☐ **P2** lib.rs:280 QR-error mapping; :756 eprintln; :803 DB cache cleanup (NOT addressed).
- ⛔ **P2** telemetry scrubber.rs LazyLock + Windows-path scrub — agent's rewrite was broken; **reverted** to clean original. (re-do cleanly later.)
- ✅ **P2** telemetry lib.rs:302 init() footgun documented.

### copypaste-relay  — committed `7944907`
- ✅ **P1** items.rs:150 — pull byte-budget cap (128 MiB) bounds lock-held cloning (DoS).
- ☐ **P1** items.rs:68 — push handler two-lock TOCTOU (NOT addressed).
- ✅ **P2** state.rs:635 — clock fail-closed; config.rs max-items clamp; rate-limit consts used.
- ✅ **P3** auth.rs — case-insensitive Bearer; routes 4xx not 5xx on bad path.
- ⛔ **P3** unauth GET /devices,/stats enumeration; TLS; request-timeout (needs dep) — deferred.

### copypaste-core  — committed `7944907`
- ✅ **P0** schema.rs:132 — user_version read propagates error (`?`).
- ✅ **P0** db.rs:541 — rekey() embeds path in error.
- ✅ **P1** pool.rs:72 — cache_size pragma added.
- ✅ **P1** items.rs:944 — FTS5 reserved-keyword strip.
- ✅ **P1** db.rs:714 — migration_v4 MAX(rowid) propagates error.
- ✅ **P1** sensitive/detector.rs:146 — degraded PATTERN_SET logs + falls back; patterns.rs error log.
- ✅ **P2** image.rs:262/297 — Err instead of panic.
- ☐ **P2** sync_key zeroize; cache_size config wiring; items two-step crash window; migration JSON parser (NOT addressed).
- ✅ CLEAN: crypto/AAD/CT-compare/HKDF/SQLi — all confirmed good.

### copypaste-sync  — committed `7944907`
- ✅ **P0** clamp_timestamps in prod ingest — fixed daemon-side (sync_orch).
- ⛔ **P0** engine.rs:264 — equal-lamport `>=` WANT (only matters if SyncEngine wired → Tier-4, skipped).
- ⛔ **P1** SyncEngine.run_session dead-code (wire-or-delete → Tier-4, deferred).
- ✅ **P1** protocol.rs:147 — encode() frame-size guard.
- ✅ **P2** engine.rs — max-lamport local dedup; post-DONE peer clock; corrected skew constant.

### copypaste-p2p  — committed `7944907`
- ⛔ **P0** transport.rs:383 — accept-loop TLS-handshake-blocking refactor (needs daemon-side change → deferred/skipped).
- ✅ **P1** interfaces.rs:170 — **HW-B2 root cause**: prefer default-route egress IP (UDP-probe).
- ✅ **P1** transport.rs:387 cache TlsAcceptor; discovery empty-label fallback; drop 0.0.0.0; PasswordFile ZeroizeOnDrop.
- ⛔ **P1** bootstrap.rs:260 bind 0.0.0.0 interface restriction (deferred); pake.rs:391 clone zeroize (not done).
- ⛔ **P2** TLS1.3-only; is_known double-check; rate-limit token; ULA/v6 (deferred).

---

## B. PER-TASK ACCEPTANCE VERIFICATION

### macOS sound/notify  — committed `7944907`
- ✅ M12 / HW-M7 — keyboard Enter routes through handleCopy; bulk copy fires sound/notify; popup pref defaults aligned to true.

### macOS logs/about  — verified PASS (no fix needed)
- ✅ A6 PASS · A7 PASS · HW-M11 PASS · HW-M9 PASS.

### macOS history view  — verified PASS
- ✅ M1, M2, M3/HW-B1, M5, A1/HW-M13, A3, HW-M12 all PASS.
- ✅ ImageThumb placeholder-on-error fixed (`7944907`). ☐ M5 true infinite-scroll, ☐ VirtualList key (key fix applied via Fragment in `7944907`).

### macOS popup  — verified PASS
- ✅ M10, M11, HW-M5, HW-M6 PASS. ✅ fallback dims fixed. ☐ popup.fixes test coverage gap (tests not added).

### macOS devices/pairing  — committed `7944907`
- ✅ A4/HW-M14, HW-M10, HW-M2, HW-B2(mac) PASS.
- ✅ A5 — "already paired" warning text **removed** (per spec).
- ✅ generateQr TOCTOU inflight guard added. ☐ expires_in_secs unwrap(0) (minor, not done).

### macOS settings  — committed `7944907`
- ✅ M4, M6, M7, M8/HW-M8, M9, HW-M4, S1, S2(UI), S4 PASS.
- ✅ HW-M3 — InfoPopover now React-portal rendered (no clipping).
- ☐ S3 — UI SI-MB vs core MiB cosmetic mismatch (not changed).

### macOS install + core defaults  — committed `7944907`
- ✅ HW-M1 PASS · S1/S3/S4 PASS · schema v8 + reorder_pinned + clear-all-preserve-pinned PASS.
- ✅ S2 — P2P default flipped ON (`unwrap_or(true)`) + daemon runtime honors config (Fix 4).

### Android capture/list  — verified PASS (`60796d0`)
- ✅ HW-A1, HW-A2, HW-A3, AND1, HW-A13, HW-A15, HW-A16 all PASS.
- 🟡 AccessibilityService onDestroy / SupervisorJob hardening (K-services). ☐ ClipboardHistoryAdapter dead-code confirm.

### Android services/sync (Kotlin)  — committed `8a5d711`
- ✅ **P0** SupabasePollWorker image-corruption — isImage branch added.
- ✅ **P1** captureImageClip privateMode guard added.
- ✅ **P1** FgsSyncLoop honors p2pSyncEnabled/syncEnabled + lamport.observe.
- ✅ **P1** storeItemWithLww TOCTOU re-check; item_id_ref cleanup in all delete/clear/prune.
- ✅ **P2** SupervisorJob; RelayClient errorStream/charset/disconnect; bumpTodayCounter lock; SecureRandom singleton; PREFS_NAME const.

### Android pairing  — committed `8a5d711`
- ✅ **P0** PairActivity duplicate LaunchedEffect removed.
- ✅ **P2** deep-link invalid-payload feedback; QR_BITMAP_PX const; blur on clip box; dead strings removed.
- ✅ HW-A4/A5/A6/A7/A12/AND2/HW-A10 already PASS (`356ea36`/`dccb340`).

### Android UI/activities (Kotlin)  — committed `8a5d711`
- ✅ **HW-A8** search-as-icon + full-width expand + recent-5 suggestions.
- ✅ **P1** HistoryActivity LaunchedEffect(item.id,expanded); remember→live settings; decode/icon failures logged; rememberSaveable dialog/menu/tab.
- ✅ **P1** MainActivity relay token=\"\" documented (dead path) + sync-setup try/catch.
- ✅ **P1** NotificationHelper POST_NOTIFICATIONS guard.
- ✅ **P1** OnboardingActivity batteryExempt live-read.
- ✅ **P1** LogViewerActivity scroll single-driver + precompiled regex; SettingsActivity logcatStatus live + rememberSaveable tab.
- ✅ **AND9** header now matches nav label ("Pair").
- ⛔ **P2** bulk hardcoded-string→resource extraction (localization polish, deferred).

### Android settings  — verified PASS (`60796d0`)
- ✅ AND3/AND4/AND5/AND6/HW-A9/HW-A14/HW-A10/HW-A17/S1/S2/S3/S4 PASS.
- ☐ preview-delay hint string; privateMode no UI; history-size no UI (K-logviewer-settings / deferred).

### Android sync/images/a11y  — partial
- ✅ AND8/AND10 PASS. 🟡 AND7 Worker image-corruption fixed (K-services). ☐ AND9 (K-main).

### Open items
- ✅ **HW-A8** — search-as-icon + full-width + recent-5 (`8a5d711`).
- ✅ **HW-A11** — copyable adb a11y command added to Permissions + Onboarding (`8a5d711`).
- ⛔ AND10 multi-device list (architectural, deferred).

### scripts/build/CI  — hardening done (commit pending); release-flow items flagged for human
- ✅ install.sh — SHA-256 sidecar verify before mount + curl|bash warning (best-effort if no sidecar).
- ✅ make_dmg.sh `set -euo pipefail`; build-android-apk.sh ./gradlew + version-from-Cargo + quoted JAVA_HOME + no hardcoded keystore pass; build-macos.sh version-from-Cargo.
- ✅ setup-tap.sh — `${{ inputs }}` moved to env: (injection fix).
- ✅ deny.toml android target; tempfile pin `>=3.0,<3.14` on daemon+cli; stale MSRV pin comments 1.86→1.89; build.gradle.kts versionCode drift note.
- ⚠️ **FLAGGED for human decision** (not changed, `# AUDIT:` comments added): release.yml `continue-on-error:true` + debug-signed APK publishing + NDK `|| true`; audit.yml `cargo audit || cargo audit`; nightly.yml `ubuntu-latest-xlarge`.

---

## TIER-4 DEFERRED (need explicit decision — NOT mechanical)
- ⛔ P2P `SyncEngine.run_session` dead-code — wire into production or delete.
- ⛔ PAKE channel binding (MitM-relay hardening).
- ⛔ Android `origin_device_id` stable identity — UDL device_id param + ABI bump + bindgen regen.
- ⛔ Relay auth model (bearer on GET /devices,/stats) + TLS.
- ⛔ TLS 1.3-only for P2P.
- ⛔ Telemetry scrubber LazyLock + Windows-path scrub (clean re-implementation).

## C. STILL-OPEN (carry-over)
- ⏳ HW-A8 (Android search) — K-history.
- ☐ HW-A11 (adb command) — K-onboarding-perms.
- ☐ HW-B2 — code root cause fixed (p2p multi-homed addr); needs **real-hardware** pairing verification.
