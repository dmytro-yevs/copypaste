# v0.5.3 Full-Codebase Audit & Per-Task Verification — Findings

> Generated 2026-06-02 by a fleet of read-only auditors (one per crate/area) + per-task
> acceptance verifiers. Triage in progress. Severity: P0 critical / P1 important / P2 minor.
> Status: COLLECTING (not all agents reported yet).

---

## A. MODULE BUG AUDITS

### copypaste-ui (React/TS + src-tauri)  [done]
- **P0** SyncStatusChip.tsx:98 — stale-closure/leak: `refresh` not in `useCallback`, setInterval with `[]` deps + eslint-disable; cancelRef can go stale → setState after reset.
- **P0** src-tauri/lib.rs:1488 — `spawn_tray_recent_resync` Phase 2 `loop { ... sleep(5s) }` never exits; holds AppHandle, blocks teardown. Needs AtomicBool stop flag set on RunEvent::Exit.
- **P0** SettingsView.tsx:795 — `handleP2pToggle` sends `{...config, p2p_enabled}` (stale config closure) instead of `buildConfigPatch(...)` → can overwrite storage/supabase fields.
- **P0** SettingsView.tsx:940 — sound/notify toggles call buildConfigPatch before loadState ready → may push wrong defaults; revert only undoes pref.
- **P1** Popup.tsx:167 — onFocusChanged unlisten teardown race (no cancelled guard).
- **P1** DevicesView.tsx:325 — loadPeers stale `ownState` closure; self-filter uses stale ownFp.
- **P1** HistoryView.tsx:983 — auto-refresh effect deps `[load,loadState]` → double-load on state recovery.
- **P1** SettingsView.tsx:809 — handleWifiOnlyToggle double-revert + wrong deps.
- **P1** SettingsView.tsx:641 — load effect missing cancelled guard; concurrent reloadKey bumps interleave setPrefs.
- **P1** LogView.tsx:40 — Object URL revoked immediately after click → download can abort.
- **P1** src-tauri/lib.rs:1235 — tray "recent:*" copy emits no sound/notification (inconsistent w/ M12).
- **P1** ImageThumb.tsx:66 — no in-flight dedup (AppIcon has it) → duplicate fetch race.
- **P2** store.ts:85 — bare catch swallows localStorage parse/write failures (settings silently don't persist).
- **P2** src-tauri/ipc.rs:74 — hardcoded request id "ui-1" (no correlation).
- **P2** src-tauri/lib.rs:877 — POPUP_W/H constants must match tauri.conf.json, no assertion.
- **P2** HistoryView.tsx:861 — module-level mutable `_toastSeq`.
- **P2** HistoryView.tsx:1041 — multi-select micro-task race can wrongly exit selection mode.
- **P2** Popup.tsx:182 — magic 50ms focus delay (HW-sensitive).
- **P2** DevicesView.tsx:403 — handleUnpair/Revoke no cancelled guard (setState after unmount).
- **P2** daemon_lifecycle.rs:514 — mutex poison silently recovered, no log.
- **P2** lib.rs:285 — set_popup_shortcut ignores unregister error (ghost shortcut).
- **P2** ipc.rs:190 — read_logs only reads latest rotated file (incomplete tail).
- **P2** SettingsView.tsx:696 — saveLimitsField reverts ALL sliders on any one error.

### copypaste-daemon  [done]
- **P0** ipc.rs:595 — local `save_peers` does fs::write THEN chmod → world-readable window for peers.json containing `sync_key_b64` (shared P2P content key). Handlers pair_peer/unpair_peer/revoke_peer/revoke_all_peers/pair_accept_finish all use the non-atomic local fn. Fix: use crate::peers::save_peers (atomic 0600).
- **P0** sync_orch.rs:427 — replace_item_atomic hardcodes key_version=CURRENT(2) for all TakeRemote merges; a legacy key_version=1 verbatim row gets stamped v2 but is encrypted under v1 → decrypt auth-tag failure. Fix: preserve wire.key_version on verbatim path.
- **P1** daemon.rs:1114 — afplay/osascript spawned, Child dropped without wait() → zombie processes on every copy.
- **P1** daemon.rs:747/809 — TTL cleanup intervals (5000ms/60000ms) duplicated as literals in 2 platform branches.
- **P1** cloud.rs:759/1572 — reqwest::Client::new() with no timeout → loops can hang indefinitely.
- **P1** ipc.rs:1837/2167 — bump_item_recency errors double-swallowed (let _ spawn_blocking + let _ inside) → promote-on-copy silently fails.
- **P1** sync_orch.rs:458 — catchup_items get_page(10_000) hardcoded → drops items >10k + materializes 10k structs.
- **P1** daemon.rs:508 / ipc.rs:3876 — device-name fallback "CopyPaste" duplicated, not a const.
- **P1** ipc.rs:3510/3780 — PAKE SessionKey discarded; no channel binding (MitM relay can pair own fingerprint). Design debt.
- **P2** daemon.rs:997 — run_degraded busy-polls 250ms forever.
- **P2** daemon.rs:1209 — ITEM_KEY_VERSION_CURRENT_U32=2 duplicates core const.
- **P2** daemon.rs:741 — sensitive_ttl_secs==0 → deletes ALL sensitive items every tick.
- **P2** cloud.rs:784 — startup backlog LIMIT 1024 silently drops items 1025+.
- **P2** sync_orch.rs:87 — shared_sync_key reads peers.json from disk per item.
- (plus several hardcoded-magic intervals)

### copypaste-supabase (+ cloud.rs consumer)  [done]
- **P0** cloud.rs:804 — backlog query `WHERE ... content_type='text'` + decrypt/build gates → image items NEVER sync (is_synced stays 0 forever). NOTE: AND7 says Android side fixed; macOS daemon→cloud upload still text-only. Verify intent.
- **P0** cloud.rs:1793 — WS reconnect always uses initial bearer JWT, never refreshed → after ~1h JWT expiry realtime dies permanently.
- **P1** auth.rs:220 — spawn_auto_refresh exported but NEVER called → no proactive token refresh.
- **P1** cloud.rs:2671 — poll loop folds 429 into generic Failed; ignores Retry-After → gaps under rate-limit.
- **P1** realtime.rs:235 — realtime subscription has no `filter` (user_id) → server delivers all rows pre-RLS.
- **P1** cloud.rs:760/1572 — two independent reqwest clients, no timeout.
- **P2** several silent error swallows (unwrap_or_default on bodies/rows), hardcoded vsn=1.0.0, POLL limit=20 literal, ws snapshot config not hot-reloaded.

---

## B. PER-TASK ACCEPTANCE VERIFICATION

### macOS sound/notify  [done]
- M12 **PARTIAL** — popup + main row-click copy fire sound/notify correctly. BUGS: keyboard Enter copy (HistoryView 1129) and bulk copy (1351) skip sound/notify. Popup inline pref defaults `false` vs store `true` (latent fresh-install mismatch).
- HW-M7 **PARTIAL** — row-click PASS; keyboard Enter path skips it.

### macOS logs/about  [done]
- A6 **PASS** (textarea wraps). A7 **PASS** (real IPC read+export; export only in-memory 500-line tail — UX note). HW-M11 **PASS** (single IconLogs, nav wired). HW-M9 **PASS** (bordered/padded row).

### copypaste-ipc + copypaste-cli  [done]
- **P0** cli common.rs:28 — days_to_ymd loops year-by-year from 1970; a near-i64::MAX wall_time hangs the CLI (~1e11 iterations). Add upper-bound guard.
- **P1** cli ipc.rs:103/143 — id-mismatch guard uses `.as_str().unwrap_or("")`; numeric daemon id → both "" → guard always passes (accepts any response).
- **P1** cli cloud.rs:125 — password Zeroizing copied into plain serde_json String (heap plaintext survives). 
- **P1** cli import.rs:29 — read_to_string entire file, no size cap → OOM + exceeds daemon 16MiB.
- **P1** cli status.rs:164 — request id "status" not "1" (breaks if daemon normalizes ids).
- **P2** cli vacuum.rs:121 wal_checkpoint error swallowed; watch.rs:62 --interval 0 spins CPU; copy.rs process::exit bypasses propagation; all command method-name strings hardcoded (no METHOD_* consts); export.rs TOCTOU; ipc Response type divergence (String vs u64 id).

### copypaste-android FFI + copypaste-telemetry  [done]
- **P0** android/src/lib.rs:937,947 — `add_clipboard_item` encrypts with AAD `build_item_aad(id, V3)` ("{id}|3") but stamps key_version=2; daemon decrypts key_version=2 with AAD "{id}|4|2" → **auth-tag mismatch, EVERY FFI-inserted item undecryptable by daemon**. (Verify whether this FFI path is actually used by Kotlin — if so it's a dual-sync breaker.) Fix: use build_item_aad_v2(id, V4, 2).
- **P1** android lib.rs:177 — derive_cloud_sync_key uses byte-len guard vs core char-count → 2-emoji passphrase passes Android, fails core; PassphraseTooShort reason lost in `_ => EncryptionFailed`.
- **P1** android lib.rs:600 — sync_with_peer stamps fresh random UUID as origin_device_id every call → daemon can't dedup/LWW → Android clips accumulate as new items on macOS. (FIXWAVE; needs UDL device_id param + ABI bump.)
- **P1** android lib.rs:856 — expect() in FFI with_cached_db (provably safe now but unwinds JVM if refactored).
- **P2** android lib.rs:280 QR-gen error mapped to DecryptionFailed; :756 eprintln! lost on Android (no logcat); :803 two DB caches no cross-cleanup; :659 key_version=2 literal; :488 P2P_SYNC_KEY_SALT hardcoded, not shared with daemon (no compile assert).
- **P2** telemetry lib.rs:302 init() always Noop regardless of consent; scrubber.rs:83 Windows C:\Users path not scrubbed; 9× Regex::new().expect() at construction (use LazyLock).

### copypaste-relay  [done]
- (P0 fail-open auth previously flagged is FIXED — verify_token fails closed, constant-time bearer compare.)
- **P1** items.rs:150 — pull_items clones full content_b64 per item while holding global Mutex → up to GBs cloned under lock = authenticated DoS. Use Arc or per-device lock or byte-budget cap.
- **P1** items.rs:68 — push handler acquires Mutex twice (verify then push) → TOCTOU evictor race → spurious 404.
- **P2** state.rs:635 clock unwrap_or_default → items stored with inserted_at=0 → pruned immediately (silent loss); rate-limit literals not using consts; RELAY_MAX_ITEMS_PER_DEVICE=0 silently drains; devices.rs:140 clock underflow.
- **P3** GET /devices, /stats, /devices/:id unauthenticated → device enumeration incl. device_name; bearer scheme case-sensitive; no TLS warning; no request timeout; orphan db.rs.

## B. PER-TASK VERIFICATION (cont.)

### macOS history view  [done — all PASS]
- M1, M2, M3/HW-B1, M5, A1/HW-M13, A3, HW-M12 all PASS with evidence.
- BUGS: M5 not true infinite scroll (single 200 page, MAX_PAGE 1000, no load-more → items >1000 silently omitted); ImageThumb error renders null (blank row, no placeholder); VirtualList key placement may remount rows on scroll.

### macOS popup  [done — all PASS]
- M10, M11, HW-M5, HW-M6 PASS. BUGS: lib.rs:1024 fallback builder still inner_size(420,520) stale; popup.fixes.test.tsx has ZERO tests for M10/M11/HW-M5/HW-M6.

### macOS devices/pairing  [done]
- A4/HW-M14 PASS, HW-M10 PASS, HW-M2 PASS, HW-B2(mac) PASS.
- A5 **PARTIAL/FAIL**: blur/reveal/regen + no Regenerate button all PASS, but the "1 device already paired … no effect" text is STILL RENDERED (DevicesView.tsx:646-654) — task said remove it. Decide: remove or keep as reworked warning.
- BUGS: expires_in_secs unwrap_or(0) silent; generateQr TOCTOU (manual 2nd-click regen races auto-refresh, wastes single-use token).

### macOS install + core defaults  [done]
- HW-M1 PASS. S1 PASS, S3 PASS, S4 PASS. Schema v8 + reorder_pinned + clear-all-preserves-pinned all PASS (daemon).
- S2 **FAIL/BUG**: core `p2p_enabled` defaults to None→`unwrap_or(false)` (ipc.rs:239) = P2P OFF by default, NOT ON as required. ALSO daemon runtime reads only env COPYPASTE_P2P, ignores persisted p2p_enabled (ipc.rs:231) → settings toggle has no runtime effect. Significant for dual-sync.

### Android capture/list  [done — all PASS]
- HW-A1, HW-A2, HW-A3, AND1, HW-A13, HW-A15, HW-A16 all PASS.
- BUGS: AccessibilityService.onDestroy swallows uninit error; MainActivity sync-manager/listener setup (112-115) NOT in try/catch (latent if constructor ever throws); ClipboardHistoryAdapter appears dead (legacy RecyclerView, no pinned/reorder) — confirm/remove; hashCode() 32-bit echo-dedup false-positive edge.

### macOS sound/notify  [done]
- M12 / HW-M7 **PARTIAL**: popup + row-click PASS; keyboard Enter (HistoryView 1129) and bulk copy (1351) skip sound/notify; popup pref inline defaults false vs store true.

### copypaste-core  [done]
- **P0** schema.rs:132 — `PRAGMA user_version` read `.unwrap_or(0)` → transient failure re-runs all migrations on a migrated DB ("duplicate column"). Use `?`.
- **P0** db.rs:541 — rekey() renames file THEN re-opens; if re-open fails, error carries no path → unrecoverable. Embed path in error.
- **P1** pool.rs:72 — pool omits `PRAGMA cache_size=-8192` (claims "kept in sync", isn't) → 2MB cache.
- **P1** items.rs:944 — FTS5 sanitizer lets NOT/OR/AND through → invalid query → search returns Err not empty.
- **P1** db.rs:714 — migration_v4 MAX(rowid) `.unwrap_or(0)` marks sweep Complete at cursor 0 → never re-runs.
- **P1** sensitive/detector.rs:146 — is_sensitive() split-brain if PATTERN_SET degrades empty → returns false for all (FTS gating). 
- **P2** sync_key.rs:537 stack key not zeroized; db.rs cache_size hardcoded ignores config; image.rs:262/297 reachable expect() panics; items.rs two-step insert+fts crash window; migration_v4 hand-rolled JSON parser.
- CLEAN: nonce reuse, weak rng, AAD binding, CT-compares, HKDF info strings, SQL injection — all good.

### copypaste-sync  [done]
- **P0** engine.rs:374 vs sync_orch — `clamp_timestamps()` NEVER called in production P2P ingest (merge_incoming_with_crypto); hostile peer sends negative lamport → casts to u64::MAX → saturates clock → wins LWW forever. Tests only cover dead SyncEngine path.
- **P0** engine.rs:264 — equal-lamport items excluded from WANT (`>` not `>=`) → device-id tie-break never fires → two peers writing same item_id at same tick diverge permanently (CRDT convergence failure). NOTE: only matters if SyncEngine is wired (see below).
- **P1** p2p.rs:708 / engine.rs — **SyncEngine.run_session() is DEAD CODE in production**; p2p uses push-only WireItem path. The whole HELLO/HAVE/WANT protocol untested in real path. Architectural: wire it or delete it.
- **P1** engine.rs:337 — outgoing ITEMS no size guard; encode() u32 len truncates >4GiB.
- **P2** stale peer last_clock; no protocol version field; MAX_WALL_TIME_SKEW comment wrong (31y vs 31,688y); content doc says base64 but serializes int-array (3-5× bloat); local_by_item_id dedup last-wins.

### copypaste-p2p  [done]
- **P0** transport.rs:383 — accept() does TCP accept + TLS handshake sequentially in accept_loop → one stalled TLS handshake blocks ALL new peers for 10s (TLS_HANDSHAKE_TIMEOUT). Spawn handshake off the accept loop.
- **P1** interfaces.rs:170 — **pick_advertise_host() returns first IPv4, nondeterministic on multi-homed hosts → advertised addr_hint unreachable from peer on different subnet = LIKELY HW-B2 ROOT CAUSE.** Prefer default-route interface.
- **P1** transport.rs:387 build_server_config rebuilt every accept; discovery.rs:255 empty sanitized label → invalid mDNS; discovery.rs:498 ScopedIp fallback 0.0.0.0 dialed; pake.rs:391 ClientLogin clone may not zeroize; pake.rs:224 PasswordFile no ZeroizeOnDrop (OPAQUE keys linger); bootstrap.rs:260 binds 0.0.0.0 no interface restriction.
- **P2** TLS1.2 enabled (could mandate 1.3); redundant is_known double-check race; rate-limit global token charged on per-key-cap drop; discovery silent exit; magic timeouts not shared with QR TTL; ULA/IPv4-mapped v6 not excluded.

### Android services/sync (Kotlin)  [done]
- **P0** SupabasePollWorker.kt:98 — 15-min WorkManager fallback decodes ALL rows as UTF-8 text → **image rows from remote silently corrupted** (FgsSyncLoop has the isImage branch, Worker doesn't). AND7 only half-fixed.
- **P1** ClipboardRepository.kt:473 — storeItemWithLww new-item path TOCTOU (lock released between lookup and insert) → FgsSyncLoop + Worker both insert same itemId = duplicate rows.
- **P1** ClipboardService.kt:480 — **captureImageClip does NOT check privateMode** → images captured + notified even in private mode (privacy leak).
- **P1** FgsSyncLoop.kt:188 — dialPairedPeer ignores settings.p2pSyncEnabled (and syncEnabled) → P2P dials even when user disabled it. (Ties to HW-A9 toggle being inert.)
- **P1** ClipboardRepository delete/clear/prune paths never remove `item_id_ref_$id` reverse key → orphan blobs + ghost LWW on re-sync.
- **P1** FgsSyncLoop.kt:347 — P2P-received items never call lamportClock.observe() → local clock lags → future pushes have stale ts → breaks LWW.
- **P2** scopes lack SupervisorJob (one crash kills all capture); bumpTodayCounter RMW race; 32-bit hashCode dedup; RelayClient errorStream NPE + platform charset + no disconnect(); SecureRandom per call; hardcoded prefs file names; dead lastLamportTs.

### Android UI/activities (Kotlin)  [done]
- **P0** PairActivity.kt:408+430 — **duplicate `LaunchedEffect(Unit)`** runs QR-gen twice (copy-paste dup). Remove the second block (430-447).
- **P1** MainActivity.kt:113 — SyncManager built with `token=""` → relay uploads 401 silently → in-activity captures never reach relay.
- **P1** NotificationHelper.kt:49 — notify() without POST_NOTIFICATIONS check → SecurityException on API33+ revoke.
- **P1** OnboardingActivity.kt:303 batteryExempt remember(ctx) never refreshes; HistoryActivity.kt:887 LaunchedEffect(expanded) leaks timer; LogViewerActivity scroll toggle race; SettingsActivity logcatStatus stale.
- **P2** MANY hardcoded user-strings not in string resources (Onboarding/Permissions/LogViewer/PairActivity); rememberSaveable missing (tab/dialog lost on rotation); decode failures swallowed; SimpleDateFormat per bind; deep-link PAKE-oracle surface (autoVerify=false); settings read via remember don't refresh live.

### scripts/build/CI  [done]
- **P0** release/install.sh — DMG not checksum-verified + curl|bash pattern (MitM RCE).
- **P0** release.yml:197 — build-android `continue-on-error:true` + ships **app-debug.apk** (debug-signed) to production release.
- **P1** build-android-apk.sh hardcoded keystore pass `copypaste-beta`; setup-tap.sh `${{ inputs }}` injection in run:; make_dmg.sh no pipefail; uses system `gradle` not `./gradlew`; ARTIFACT_NAME + version hardcoded stale (v0.2.0); NDK install `|| true` silent; tempfile dev-dep unpinned vs <3.14 cap.
- **P2** deny.toml missing android target; nightly xlarge runner; GH_TOKEN in remote URL; NDK version duped; android versionCode/Name not from Cargo.toml; cargo-audit `|| ` masks failure; tauri version sed not jq; stale pin comments (MSRV 1.86 vs 1.89).

## B. PER-TASK VERIFICATION (cont. 2)

### macOS settings  [done]
- M4 PASS, M6 PASS, M7 PASS, M8/HW-M8 PASS, M9 PASS, HW-M4 PASS, S1 PASS, S2 PASS (UI), S4 PASS.
- HW-M3 **PARTIAL/BUG**: split outer/inner div is architecturally INCOMPLETE — InfoPopover still descends from inner `overflow-hidden` div, no React portal → still clipped when popover overflows panel. Needs createPortal or remove inner overflow-hidden.
- S3 **PARTIAL**: UI labels SI MB (10,000,000) but core default is 10 MiB (10,485,760) — first-load IPC may send 10MB SI < core default. Cosmetic unit mismatch.

### Android settings  [done — all PASS]
- AND3,AND4,AND5,AND6,HW-A9,HW-A14,HW-A10,HW-A17,S1,S2,S3,S4 all PASS.
- BUGS: preview-delay hint string "200–100000ms" stale vs UI cap 30000; privateMode pref has NO ui control (conflated with captureEnabled label); historySize/maxHistoryItems no UI.

### Android pairing  [done — all PASS]
- HW-A4,HW-A5,HW-A6,HW-A7,HW-A12,AND2,HW-A10 all PASS. BUGS: duplicate LaunchedEffect (see P0 above); dead strings btn_pair_start/regenerate; malformed deep-link no user feedback.

### Android sync/images/a11y  [done]
- AND8 PASS, AND10 PASS (single-peer scope). AND7 **PARTIAL** (Worker image corruption P0 above). AND9 **PARTIAL** (nav "Pair" vs header "Pair Device").

### Open items  [done]
- HW-A8 **FAIL** — search is fixed 160dp field always shown; no icon toggle, no expand, no recent-5. Fix in HistoryActivity TopAppBar.
- HW-A11 **FAIL** — NO adb accessibility command shown anywhere. Correct: `adb shell settings put secure enabled_accessibility_services com.copypaste.android/.ClipboardAccessibilityService`. Add copyable block to PermissionsSettingsActivity + OnboardingActivity.
- AND10 parity note: Android has no multi-device list (single paired peer only); macOS shows all.

---

## TRIAGE — recommended fix priority for THIS release

**TIER 1 (correctness/security that breaks dual-sync or privacy — fix now):**
1. android FFI AAD mismatch (android/lib.rs:937) IF add_clipboard_item is used by Kotlin — verify first.
2. daemon sync_orch.rs:427 key_version hardcode (legacy rows undecryptable).
3. sync clamp_timestamps never called in prod ingest (sync_orch) — hostile lamport.
4. Android captureImageClip ignores privateMode (privacy).
5. Android SupabasePollWorker image corruption (AND7 fallback).
6. Android FgsSyncLoop honor p2pSyncEnabled/syncEnabled + lamport.observe on P2P recv.
7. daemon save_peers world-readable sync-key window.
8. S2: P2P default ON + toggle actually drives runtime (daemon reads config not just env).
9. PairActivity duplicate LaunchedEffect; MainActivity relay token="".

**TIER 2 (task-FAILs / completeness vs the backlog):**
10. HW-A8 search-as-icon + recent searches.
11. HW-A11 ADB accessibility command.
12. A5 macOS "already paired" text decision (remove or keep).
13. M12/HW-M7 keyboard Enter + bulk copy sound/notify.
14. HW-M3 popover portal fix. AND9 header title. macOS sound/notify popup default mismatch.

**TIER 3 (robustness P1/P2 hardening — many items):** core unwrap_or(0) migration guards, FTS5 keyword strip, p2p multi-homed addr selection (HW-B2!), relay DoS clone-under-lock, reqwest timeouts, zombie afplay, swallowed errors, hardcoded magics, string resources, rememberSaveable, CI debug-APK/checksum.

**TIER 4 (architectural decisions — NOT mechanical):** P2P SyncEngine dead-code (wire or delete), PAKE channel binding, Android origin_device_id stable id (UDL/ABI bump), relay unauth enumeration, TLS1.3-only.

---

## C. STILL-OPEN BACKLOG ITEMS (not yet fixed)
- HW-A8 — search-as-icon + recent searches (Android) — FAIL confirmed
- HW-A11 — full ADB command, copyable (Android) — FAIL confirmed
- HW-B2 — end-to-end pairing; likely root cause = p2p multi-homed addr selection (interfaces.rs:170); needs real-hardware verification
