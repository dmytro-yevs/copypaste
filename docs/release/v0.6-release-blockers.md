# v0.6 Release Blockers (audit 2026-06-03 @ d4a74b2)

> From the opus release-readiness audit. M2(drag)/M4(tray) were FALSE POSITIVES (already merged —
> stale tracker). **DECISION: files are NOT descoped — user requires image+file sync both directions;
> files get finished, not cut.**

## P0 — must fix before shipping
- **P0-1 Android UniFFI bindings stale vs UDL.** `.udl` has `SyncedItem` 7 fields (file_name/mime, ABI 7)
  but committed `copypaste_android.kt` has 5 → RustBuffer layout mismatch → P2P sync mis-deserializes/crashes.
  FIX: run `scripts/generate-android-bindings.sh` + commit. **Do AFTER all Android-crate/UDL changes this
  cycle land (31c, file inbound) so it's regenerated once against the final UDL.** Highest-leverage.
- **P0-2 macOS file copy-back pastes encrypted garbage.** `write_to_pasteboard` (ipc.rs:5049) has no `file`
  branch → raw encrypted `content` to pasteboard. FIX: decode_file → temp file → `public.file-url` NSURL.
- **P0-3 synced files unusable e2e.** macOS no file preview/getItemFile (file-upload P3 in flight fixes
  capture+get_item_file); Android stores `[file]` placeholder + drops bytes (FgsSyncLoop:458; WS/cloud
  inbound don't handle file). FIX: macOS file display UI + Android file store/display + Android cloud/WS
  file inbound. (Keep — user wants files.)
- **P0-4 Android image copy-back broken.** HistoryActivity onCopy (~:997) always newPlainText → copies
  `[image]` text. FIX: image branch → FileProvider content URI via ClipData.newUri.
- **P0-5 synced images get NO thumbnail on Android.** All 3 inbound paths (FgsSyncLoop:321 cloud,
  FgsSyncLoop:444 P2P, SupabaseRealtimeClient:505 WS) call storeImageBytes but never
  generateThumbnail/storeThumbnailBytes → full-res in list = defeats the memory fix. FIX: regen thumb
  after storeImageBytes in all 3 inbound paths.
- **P0-6 Public/global IP collected+shown NOWHERE** (B1 requirement, both platforms). FIX: collect via
  STUN/HTTPS-echo, add field to device card on macOS + Android.
- **P0-7 Android device management absent** (parity). No DevicesActivity/Unpair/Revoke/online dot.
  IN PROGRESS: 31c agent building it.

## P1 — important
- ~~P1-2 M2 window drag~~ DONE (merged, decorations:true). ~~P1-3 M4 private-tray~~ DONE (merged, listener).
- **P1-1** macOS file rows render as `[image — id:…]` (ipc.rs:2726) → add `[file]` preview branch.
- **P1-4** `two_daemons_complete_pake_over_network_bootstrap` (pairing_network.rs:78) NOT #[ignore]d →
  will fail CI on hosts w/o multicast. `#[ignore]` it (others already are). `tls_handshake_timeout_after_10s`
  = racy paused-clock test-infra artifact, logic sound.
- **P1-5** Version bumps: Cargo workspace 0.5.5→0.6.0 (Cargo.toml:19), tauri.conf.json:4, Android
  versionName 0.5.3→0.6.0 + versionCode 8→9 (build.gradle.kts:94), cask 0.5.5 + new sha256. Write v0.6 CHANGELOG.

## P2 — polish
- **P2-1** File-size ceilings incoherent: default 1GiB (defaults.rs:13) vs MAX_FILE_BYTES 100MiB (file.rs:23)
  vs SYNC_MAX_BLOB_BYTES 8MiB (sync_orch.rs:576) vs relay 10MiB. >8MiB stores but silently won't sync
  (warn only). Pick one coherent ceiling; document in known-issues.
- **P2-2** LAN discovery + SAS pairing half-landed: primitive shipped (derive_sas, run_with_confirm); Phases
  0/2/3/4 absent (list_discovered, pairing_sm, UIs). QUEUED (LAN/SAS P0/P2-5) — user wants it.
- **P2-3** WS catch-up poll only logs row count (SupabaseRealtimeClient:540) — wire it to ingest. Minor.
- **P2-4** Relay incoming path @Deprecated/throws by design; relay SSE (#26) is the user-requested re-enable.

## Dual-sync state (audit)
Text: solid both directions (P2P + cloud + WS). Image: syncs both ways; Android display full-res (P0-5) +
copy-back broken (P0-4). File: transported but unusable (P0-2/3). Rekey crypto in sync_orch.rs verified
sound (v1-seed decode, AAD=item_id, hard-drop >ceiling, serde-default back-compat, deterministic file_id).

## Minimum path to full v0.6 (ordered)
1. Finish files (capture done via P3; + macOS copy-back P0-2 + macOS display + Android file store/display
   + Android cloud/WS file inbound P0-3). 2. Android synced-image thumbnails (P0-5) + image copy-back (P0-4).
3. B1: public IP (P0-6) + Android device screen (P0-7/31c). 4. LAN/SAS Phases 0/2-4 + relay SSE.
5. Regenerate Android bindings (P0-1) LAST after UDL final. 6. #[ignore] network test (P1-4).
7. Version bumps + CHANGELOG (P1-5). 8. Document ceiling (P2-1). 9. Merge to main + tag + signed artifacts + HW sign-off.
