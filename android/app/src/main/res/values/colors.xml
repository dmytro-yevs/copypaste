<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Adaptive launcher icon background (white, icon motif carries the brand color) -->
    <color name="ic_launcher_background">#FFFFFF</color>

    <!--
        Design System v2 "Quiet Precision" §0 canonical ramp — single source of
        truth shared with Color.kt and tailwind.config.js. Used by the AppCompat
        base theme so the pre-Compose window (splash / windowBackground) and the
        system bars match the dark Compose UI — no color flash at cold launch.
    -->
    <color name="ide_bg">#13141A</color>
    <color name="ide_panel">#1B1C22</color>
    <color name="ide_raised">#2D2F34</color>
    <color name="ide_border">#383B42</color>
    <color name="ide_text">#E8EAED</color>
    <color name="ide_accent">#3D8BFF</color>
    <color name="ide_accent_dark">#2E6FD6</color>
</resources>
